# Lessons (self-refinement ledger)

One lesson per line. Format: `- [id] (kind) lesson`. These are durable, evidence-backed
corrections and tactics; more specific lessons take precedence over broader ones.

- [20260908-i542] (tactic) Appending markdown plan chunks via `cat >>` merges the prior chunk's trailing `---` separator with the next chunk's `### Task N` heading (no newline) → grep -c '^### Task' undercounts and the plan looks incomplete. After EVERY multi-chunk append, repair with: replace lines starting `---### Task` → `---`, empty line, then the heading minus `---`. Then recount `^### Task` and scan for `Task 1[0-9].*Task` leftovers. Write each OpenVim/plan chunk with the `write` tool using a `const c = String.raw\`...\`` template, ESCAPING every backtick as `\`` (fences become `\`\`\`go`) — a single unescaped backtick (e.g. the token `\`*bytes.Buffer*\`` or `\`snap\`/init\``) throws a JS parse error ('Expected a semicolon' / 'Expected unicode escape'). ipython + Python is the fallback for content that keeps tripping the JS parser.
- [20260908-iktl] (tactic) When appending markdown plan chunks with cat-append, the prior chunk's trailing '---' separator merges with the next chunk's '### Task N' heading (no newline), so grep -c on '^### Task' undercounts. After every multi-chunk append, repair lines starting '---###' into '---' + blank + heading, then recount task headings. Write plan chunks with the write tool using a String.raw template literal, escaping every backtick as backslash-backtick; a single unescaped backtick (e.g. the token star-escape, or a backtick-slash pair) throws a JS parse error ('Expected a semicolon' / 'Expected unicode escape'). Use ipython + Python to write the chunk file when content keeps tripping the JS parser.
- [20260908-perf] (fact) P0 PERF DEBT (user: tests must be FAST — migrating from Python is partly for test speed). Fixed 2026-09-08: the 1002-log flood test was O(n^2) (each Log re-reads/re-validates/re-dumps the whole growing state file — inherent to the ported design, Python does the same). Extracted the tail rule to pure tailEvents() in eventlog.go; TestStateMirrorTail now logs 10 events (mirror==full log), TestTailEventsCap checks the 1000-boundary in-memory. Full suite 6.0s->0.3s, -race 35.9s->1.6s. REMAINING debt, deferred: (1) validation.ParseOrdered (stdlib json.Decoder token loop) costs ~4.0ms per 275KB state vs ~1ms for CPython's C parser — profiled in .scratch/profile-log.go (ReadJson 4.0ms + Validate 2.0ms per late Log at ~900 events); real campaign sizes are far smaller so it's acceptable for P0, but if the P1 CLI feels it, a hand-rolled ordered parser or pre-scan fast path is the fix — the byte-exactness contract (key order + int/float distinction) must be preserved. (2) go test -race compiles from scratch each call in this sandbox (GOCACHE under .scratch); ~1s overhead per package is normal. (3) Keep any new flood-style test in-memory (pure function) or <=~50 log calls; never put a >1000-event integration loop in the default suite.
- [20260908-azmo] (tactic) Task 13 audit: (1) The harness BANS in-package tests importing other websec packages AND bans audit<->sections cycles; external *_test packages fail to resolve symbols. Fix: keep audit_test in-package (package audit), split the six producers into package sections that do NOT import audit (sections define their own KV in sections/helpers.go + RegisterAll(register func) callback), and audit.go imports sections + exposes idempotent audit.Setup() (top-level cross-package calls are banned) that the CLI/probe must call once before AuditCampaign. (2) AuditCampaign report key order is contractual (event_log..snapshots = Python code order, NOT alphabetical); never serialize the report with CanonCompact (it sorts keys) — use validation.DumpIndented (order-preserving, json.dumps indent=2).
- [20260908-slv2] (correction) workflow agent() calls for delegation MUST include explicit provider:"openrouter", model:"meta/muse-spark-1.3-contributor" (user directive 2026-09-08) — omitting them silently falls back to the local lamma.cpp model, which cannot afford concurrent subagents; fallback model openrouter/deepseek/deepseek-v4-flash-0731.
- [20260908-a2fd] (fact) P1 findings/sandbox port seams: findings constants use the Python names (EVIDENCE_TYPE_GROUPS, ECONOMIC_CONFIRMATION_CLASSES, CROSS_CHAIN_E6_CLASSES, ALLOWED_TRANSITIONS + TransitionAllowed); RequiredLevelForCampaign goes through the findings.SetEffectiveFloor seam (floors.go Task 3 wires it); Clause.Types is map[string]struct{} with nil = any type; sandbox.ExecOutput/ExecOutputProblem/LooksLikeForgeTest/ForgeTestSummary/E4Profiles are the P2 slice findings.add_evidence needs. KNOWN PARITY GAPS: Go strings.ToLower is simple case mapping while Python str.lower() is full mapping (U+0130, final sigma) -> signatures with those chars differ; Python int counters are unbounded where Go saturates int64. Both documented in code; fix the case one with x/text/cases if a real campaign ever hits it.
- [20260908-tvwq] (tactic) Byte-exact Python->Go port verification (used for internal/dedup, reusable for remaining ports): generate ONE Python script under .scratch/<task>/templates.py that replays every scenario against the real Python module and dumps json.dumps(sort_keys=True,separators=(",",":")) canon of each object, replacing run-random ids with tokens (<A>/<B>/<LIN>) via tpl(rep,(id,token),...). In Go, assertCanon(t,label,value,want,tok{id,"<A>"},...) = CanonCompact(value) then strings.ReplaceAll per tok. This pins whole-report key sets, per-finding dedup/dedup_meta, event data, error texts and insertion ORDER for free; regenerate the templates file (never hand-edit expected strings) whenever a scenario is added. Also assert exact validation error text (Go's message matched Python's SchemaError byte-for-byte, incl. dict repr and '(+0 more errors)').
- [20260908-wh4n] (fact) web3sec-go port conventions (verified 2026-09-08 while porting risk.py/pricing.py): (1) test files in package pricing/risk must NOT declare objAt/objStr helpers — production code already defines them in-package; (2) findings.NewFindingID uses crypto/rand and ignores WEBV2_UUID while every other id goes through state.NewID, so campaign-level Go/Python twin diffs must normalize F-<12hex>; (3) int-vs-float identity is observable in event logs (Python logs the raw kwarg), so risk.EconomicRisk/RecordEconomicImpact take validation.Value, not *float64; (4) ported Python tests must be recorded by converting their testmap.json deferred-P{n} rows to real go_file/go_func rows, then run scripts/check-testmap.py; (5) real divergences go in KNOWN_DIVERGENCES.md (What/Why/Golden/Unblocks) — the ASCII \b workaround [^\p{L}\p{N}\p{Pc}] misses boundaries Python sees at U+0345/U+203F/U+2054.
- [20260908-3n2a] (tactic) Cross-twin parity probe normalization (web3sec-go, used by .scratch/parity-task2.py): finding ids come from crypto/rand on BOTH sides, so map them to [F1]/[F2] by first-seen order in events.jsonl (finding.ingested refs) — file-name sort is random and useless; normalize event_hash/prev_hash to [H] because the chain covers the ids you just rewrote; seed registries the gate reads in POST-migration shape (test_status present, e.g. {"status":"UNVERIFIED","test_status":"untested","source":"documented","findings":[],"tests":[],"detectors":[]}) or Python's _migrate_legacy_entries logs an extra invariant.migrated event the Go twin won't have; wire the unported modules (shared_memory/invariants/taxonomy/planner) through the package Set* seams in the Go twin so both sides consult the same files.
- [20260908-nfqs] (tactic) web3sec Go port: Python error-text convention — Go error = str(exception): KeyError renders as PyReprStr(args[0]) (so an implicit dict key gives 'path', an explicit raise KeyError(f"...") gives a double-quoted message), SchemaError as its message. Oracle generators must store str(exc), not exc.args[0]. Also: oracle seeds must be the on-disk FILE TEXT captured BEFORE the call (sorted-key canon reorders insertion-order JSON and breaks byte comparison).
- [20260908-d2p7] (fact) web3sec-go internal/pipeline (T11): oracle testdata (stages/joins/topo/joinsem/messages/money/scenarios.json) is generated by `.scratch/t11/gen-vectors.py` run from web3sec-final with PYTHONPATH=src; cross-twin probe is `.scratch/t11/probe/{probe_python.py,main.go,compare.py}` (4 scenarios, byte-identical) and `.scratch/t11/style-check.py` enforces TigerStyle. State/event comparisons MUST normalize the D3 environment_hash/manifest_hash (python-vs-go runtime fingerprint) — same in probe and tests.
- [20260908-1cw0] (tactic) Python→Go port verification recipe that worked for internal/planner: (1) generate oracle vectors from the Python source into testdata JSON (byte-exact expected outputs), (2) write 1:1 port tests against those vectors, (3) run a cross-implementation parity probe — same scripted scenario in both languages with WEBV2_NOW/WEBV2_UUID pinned, each step snapshotted as canonical JSON AT CAPTURE TIME (in-place plan mutation otherwise rewrites earlier steps via aliasing) and the temp root normalized to <ROOT>, then diff step-by-step. Fixtures live in .scratch/t9/ (gen-vectors.py, parity-go/main.go, parity.py, parity-probe.sh, report.json).
- [20260908-qf2i] (fact) web3sec-go planner port is BEHIND the Python twin on one string: internal/planner/gates.go says "plan predates the lens era — re-save it (webv2 plan C plan.json)" while planner.py:440 (commit bbb3eea) says "... plan.json --rebuild; the outgoing plan is archived as plan.superseded)"; its testdata/oracles.json still pins the old text. Any completion/discovery golden vector that hits the no-lenses divergence branch will fail on this — keep fixtures on the lens branch until the planner is re-synced (task 12 finding, 2026-09-02).
- [20260908-6q5d] (tactic) Cross-twin (Python vs Go) byte-exact golden vectors need four pins: WEBV2_NOW/WEBV2_UUID env + per-scenario id-stream rewind (state.ResetIDStream / ST._UUID_PIN_COUNTER=0), finding ids pinned in BOTH twins (uuid4 is random - Go findings.SetFindingIDSource, Python F.new_finding_id), the SAME absolute replay root (event hashes cover artifact paths), and JSON compared with validation.DumpIndented (CanSpaced sorts keys). Unported-module seams (structural_index, adapter.build_context, reproduction, fork_poc) are recorded from the Python run and injected via SetXxx installers.
- [20260908-jrcv] (tactic) web3sec Python→Go port: the named commits are a starting point, not the target — re-read LIVE Python at HEAD before porting (B3 task named ecea2b9+5df013a, but HEAD 7e341ee had narrowed nondiscriminative_classes to the catch-all only and split corpus.gap reasons by reason_code). Also: .scratch parity probes embed stale section/summary expectations — when the port gains an audit section, update parity-task13.py/parity-task14.py to compare the shared section set (they compared a P0 filter and failed on the new `unpriceable`). Rebuild .scratch/webv2 before running parity-task14 (stale binary fakes a regression).
- [20260908-4ll9] (tactic) web3sec-go cross-twin oracle generation: webv2 state.register_artifact logs the path EXACTLY as passed (event data.path), so an absolute tempfile path makes committed fixtures + hash chains nondeterministic (md5 changes every regen). Pass a campaign-relative path (chdir to c.dir, Path("artifacts")/name) → byte-reproducible vectors. Verify with two consecutive generator runs + md5sum. Harness: .scratch/t15/gen_vectors.py (oracle) + .scratch/t15/parity_check.py (live two-twin diff; Go half = internal/audit/p1_parity_dump_test.go gated by T15_OUT).
- [20260908-f62i] (tactic) T15 parity probing: the read-only Python reference (web3sec-final) is LIVE — re-capture reference outputs at probe time AND check `git log --oneline <baseline>..HEAD` before claiming parity; a reference commit landed 8 min after my first capture and silently invalidated a 42/42 result (turned out to be 52/53). Keep a per-step py.json + a capture.py PYTHONPATH override so any reference commit can be replayed via `git archive <sha> src | tar -x -C .scratch/...`.
- [20260908-xqmu] (tactic) Golden cross-twin determinism (web3sec py↔go) needs four pins at once: (1) finding ids — Python findings.new_finding_id uses raw uuid4 and ignores WEBV2_UUID, so pin via scripts/golden/sitecustomize.py shim + Go WEBV2_FINDING_IDS=pin with a per-step WEBV2_FINDING_ID_SEQ base; (2) the SAME run root for both twins sequentially (event hashes cover absolute artifact paths); (3) WEBV2_GLOBAL_MEMORY_DIR at an empty dir (operator ~/.webv2/shared-memory leaks into recall); (4) target materialized OUTSIDE any git repo (git-clean ladder embeds a per-process gitdir). Verify teeth by mutating a Go output string and confirming GOLDEN RED.
- [20260908-eq3m] (fact) webv2 reference bug (2026-09-08, confirmed): `resolve-candidate --note` always exits 2 — dedup.py writes dedup_meta.candidate_notes as a dict {other_fid: note} but finding.schema.json declares additionalProperties {"type":"string"}. Golden recipes must omit --note; do not "fix" the reference.
- [20260909-fxcz] (policy) User directive (2026-09-09): the web3sec-final pytest suite is too heavy/memory-hungry — NEVER run it from subagents (not even targeted single-test runs; reference checks = PYTHONPATH=src python3 -c snippets only). Only the ORCHESTRATOR may run it, and only when genuinely necessary (e.g. a phase-gate reference baseline after the Python twin moved). scripts/verify-full.sh step 7 (reference pytest) is opt-in via WEBV2_REF_PYTEST=1 (skip+note by default).
- [20260909-xwsy] (tactic) web3sec-go CLI argparse parity: error precedence is -h > option-missing-value (a value token that starts with '-' but isn't bare '-' counts as missing) > missing-required (declaration order) > unrecognized-arguments (argv order, joined by spaces, reported against the ROOT usage); options seen before a subcommand token are forwarded to the chosen subparser (e.g. `sequence --bogus run` reports run's missing-required). Collect unknowns during parsing and check them only after missing-required; verify with a cross-twin probe comparing exit+stdout+stderr against live Python (`.scratch/t22/parity/cli_parity.py`).
- [20260909-hun3] (tactic) web3sec-go argparse porting (T23 verified): a token is an OPTION only if len>1 && starts with "-" (lone "-" is a positional); a value slot rejects an option-looking token ("--reason -foo" -> "argument --reason: expected one argument", exit 2) UNLESS it matches argparse's negative-number rule (char after "-" is a digit or "." — so "-5", "-.5" AND "-1e3" are values); abbreviations (--ext/--c) are NOT implemented anywhere in internal/cli, a port-wide deviation.
- [20260909-dc0f] (policy) User directives (2026-09-09): (1) if deepseek-v4.1-flash runs out of credits, switch subagent model to openrouter/meta/muse-spark-1.3-contributor (workflow agent() model override "meta/muse-spark-1.3-contributor", provider "openrouter"). (2) give subagents maximum context up front — use the repo_map tool on both twins and embed the relevant map in their prompts instead of making them rediscover the tree.
- [20260909-jj1d] (fact) Golden suite is single-instance: scripts/golden.sh wipes .scratch/golden (spec.json, tree-py/tree-go) at start, so two concurrent runs (parallel sibling agents) make check-golden.py die with FileNotFoundError spec.json. Check `ps aux | grep golden-run` before running, or coordinate; the run itself may have completed byte-MATCH while the verdict step races.
- [20260909-aret] (tactic) T26 (web3sec-go): cross-twin parity probes MUST run both twins in the SAME root path (sequentially, snapshotting each tree) — events.jsonl/manifest hashes cover absolute paths, so different roots make every hash diverge for non-bug reasons. Also: argparse treats `-1`/`-1.5` as positionals/values unless the parser declares negative-looking options (see cli.isNegNumberCLI); and json.dumps(sort_keys=True) (validation.Canon) and sort_keys=False (validation.DumpsOrdered) are two different encoders — costs.jsonl needs the latter.
- [20260909-rr73] (tactic) web3sec-go argparse NESTED subparsers (T25, 17152/17152 3-token argv byte-exact): (1) parent-level extras seen before the subcommand token merge with the subparser's own extras and are reported as ONE "unrecognized arguments: ..." against the ROOT usage, parent entries first in argv order — but only AFTER the subparser's help (wins) and its hard errors; the command body must NOT run first (parse_args precedes dispatch), so the spec needs a deferred-extras mode (argSpec.deferExtras) instead of reporting extras inside parse(); (2) a `--` is swallowed by an adjacent positional's `-*A-*` pattern span only when that positional was matched after the last recognized option (`baseline remove NAME --` absorbed; `baseline list --` and `index c --src x --` reported). Verify with a 3-token differential fuzz (.scratch/t25/argfuzz3.py) — 2-token products miss the parent-extras ordering bug entirely.
- [20260909-4h1x] (fact) web3sec-go porting trap: Python capabilities.normalize_labels runs str() over EVERY element, so a finding's dict preconditions become repr-string labels ('{''kind'': ''state'', ...}'); Go must stringify non-string list elements (validation.PyRepr) in capabilities.Required/Granted — dropping them silently changes resemble/signature output. Caught by the T28 cross-twin probe.
- [20260909-ds7l] (tactic) web3sec-go cross-twin probes: importlib-load scripts/golden-run.py (set module WORK/GOBIN) to reuse its pinned clock (WEBV2_NOW), per-step id seed, gate-pass payload render and build, then drive a custom recipe through both twins under the SAME root sequentially and byte-diff captures + campaign tree; add a mutual-readability phase (both binaries read each archived tree) for store/relations round-trips. Working example: .scratch/t28/cross_twin.py.
- [20260909-bhd0] (fact) web3sec-go T29 probes.py port DONE (2026-09-09): internal/probes/{registry,trust,index,merge,collapse,surface,closure,shape,campaign,blanks,emit,audit,wire,probe_*}.go + internal/cli/cmd_probes.go; 69 probes tests + 36 CLI tests pass, parity 110/110 vs .scratch/t29/pyref goldens (now pinned in internal/probes/testdata/golden via parity_test.go), scripts/golden.sh GREEN. Three parity traps found: (1) twin/drift harnesses MUST replay both implementations over the SAME absolute root path sequentially (artifact+event hashes cover paths) and diff the whole campaign tree by sha256, not just stdout — that is what caught surface registration passing the count as note instead of refresh reason; (2) any by_lens/by_axis map built by ranging a Go map is nondeterministic where Python's registered_axes() is sorted-by-axis (sorted-last axis wins the lens fallback) — use sortedKeys(reg); (3) planner.probeview.go hands AxisSurfaceBlocker a pointer to the zero Value for a missing map key, so closure.go must normalize non-Obj -> nil to match Python's (blanks or {}).get(ax). NOT ported: 9 tests/test_probes_cli.py rows needing brief/report/next-actions/attention; testmap rows still deferred-P3 (flip is out of scope).
- [20260909-aasn] (tactic) web3sec-go argparse int parity (T30 `run --max-stages`, 2026-09-09): Python argparse type=int is UNBOUNDED and accepts underscore literals ("1_000"), so strconv.ParseInt diverges on both (Go exit 2 "invalid int value" where Python ran; Python's max_stages halt echoes f"max_stages={n}" with the huge value). Fix in the verb, not the shared pipeline: parse with math/big + pyDigits (single underscores between digits), clamp out-of-int64 to MaxInt64/MinInt64 for the pipeline, and after Run rewrite summary["halt"] to the exact Python digits when the clamped echo fired — observationally identical because a budget beyond 2**63 stages can never be consumed. Edge table lives in .scratch/t30/xc_run.py EDGE_ARGS (22 argv shapes) and internal/cli/cmd_run_test.go TestPyIntMatchesArgparseInt.
- [20260909-9u34] (fact) (fact) web3sec-go audit registry trap: AuditCampaign(c) iterates the registry populated by audit.Setup(); calling it WITHOUT Setup() yields sections={}, ok=true — a silently GREEN audit. briefing.BuildBrief's deep branch hit this (brief --deep reported ok:true/0 problems while `audit --json` reported ok:false). Fix 2026-09-09: call audit.Setup() at the top of any library path that audits (BuildBrief deep branch does now). Rule: every new caller of audit.AuditCampaign must call audit.Setup() first; only cmd_audit.go and tests did before.
- [20260909-3wpy] (tactic) (tactic) web3sec-go testmap.json flipping (T31, 2026-09-09): a row becomes "real" by filling go_file/go_func AND DELETING its "status" key (deferred-Pn) — check-testmap.py counts real rows as those without "status", so leaving status makes the ported test invisible to the reconciliation summary (67 flipped rows showed deferred-P3=267 until status was removed → 200). Always run scripts/check-testmap.py after flipping and confirm go_file/go_func refs resolve (it indexes every internal/**/*_test.go func).
- [20260909-bm6p] (tactic) Port-parity beyond tests: dump json.dumps(load_records(), sort_keys=True, ensure_ascii=True) from the LIVE Python module and validation.Canon(v,false) from the Go port, then cmp bytes. On T34 this 12.6MB digest (930+3590+555 records) caught a scabench compose_root_cause bug (Go TrimSpace vs Python strip(" .") keeping an interior newline) that the 34 ported Python tests missed. Harnesses: .scratch/t34/pydigest.py + .scratch/t34/digest/main.go + .scratch/t34/cross_twin.py.
- [20260909-xafa] (fact) websec twins: finding ids (new_finding_id / findings.NewFindingID) are a raw uuid4 in BOTH twins and are NOT pinned by WEBV2_UUID (only state.new_id is) — so any cross-twin analysis whose result depends on chain/priority cut ORDER (e.g. chains cap, scripts/cap-analysis.py) is a per-run value (overlap 275-373/500 observed); assert properties (cap=500, 793-member space, subsets, seed-dependence) not digests. Pin WEBV2_NOW + WEBV2_UUID per subcommand to stabilize everything else.
- [20260909-42e8] (fact) Golden v5 (T38, 2026-09-09): scripts/golden.sh = 179 steps x 2 twins, 68 tree files (2 campaigns + sft-store/examples.json). The P4 fixture scripts/golden/p4 (built/checked by build.py, 29 files, 440KB) backs all three seams for BOTH twins: WEBV2_POC_ROOT=scripts/golden/p4/datasets, WEBV2_EVAL_DIR=scripts/golden/p4/eval, WEBV2_SFT_STORE=<root>/sft-store/examples.json (per-twin copy — sft split rewrites it). Python side needs scripts/golden/sitecustomize.py patches (sft_dataset.store_path is a FUNCTION, patched via module attribute). Byte-vs-rune clipping is the recurring port bug class: Python `s[:n]` is CHARACTERS — fixed in roles/context.go, corpus/report.go, sft/backfill.go, adapter.go (D29); any new `len(s) > n; s[:n]` on non-ASCII text is a divergence.
- [20260909-2vau] (policy) User directive (2026-09-09): spawn subagents with model deepseek-official/deepseek-v4.1-flash-expires-on-0910 (expires 09-10). Plain `subagent` has no model param — route via `workflow` agent() with the model override. Supersedes the earlier z-ai/glm-5.3-flash delegation policy.
- [20260910-lean] (fact) web3sec-go leanness wave F + D8 LANDED (2026-09-10, commits 8c2864d, dd08da5 + the D8 commit): the Python twin retired, so the Go tree stopped paying for it — `internal/routing`, `internal/datasets/forge`, `internal/datasets/scabench`, the twin-diff harnesses and every "PYTHON WINS" doctrine header are gone or archived under `docs/archive` / `scripts/archive`; 17 per-package `setOrAppend`/5 `setDefault`/3 `writeU4` copies collapsed into `internal/validation` (kvutil.go); `chainengine.dump`≡`validation.CanonSpaced` and `cli.prettyASCII`≡`validation.DumpIndentedASCII` merged behind PERMANENT equivalence tests (190,476 + 126,987 sampled values — a merged emitter is only safe with a test that proves the merge); `assets/testdata/asset_manifest.json` (95 files, 6 packs, `scripts/sync-asset-manifest.py`) pins every embedded pack — RE-SYNC it after touching any asset. D8: `poc_requirements.patch_clause` = verification|prose|none (absent = verification ⇒ golden/pinned vectors byte-identical) switches `check12`; prose reads `verification.recommendation` (≥40 runes) and the boundary-mutation record becomes an advisory. Advisories (`bounty.advisories`) were written by the gate and read by NOBODY — now rendered by report.go (`  - advisory: …`) and `gate` (`  advisory: …`), emitted only when non-empty so no output moves.
- [20260910-wk13] (correction) verify-full.sh step 13 (runbook walkthrough) shipped with `$WORK` — a variable that never existed in that script — and the header still said "step 13/12". It looked fine because the walkthrough had been verified by running `scripts/runbook-walkthrough.sh` DIRECTLY, never through the gate that now wraps it. Adding a step to a numbered gate script means: bump `TOTAL_STEPS`, use a path you actually created (`mkdir -p "$ROOT/.scratch"`), and run the whole script end to end before claiming green — `bash -n` cannot see an unbound variable under `set -u`.
- [20260910-4kss] (tactic) web3sec-go leanness audits must sweep for DORMANT coverage, not just dead code: grep every env-gated skip (`os.Getenv("WEBV2_...")`) and prove SOMETHING sets it. 2026-09-10 second pass found (a) two twin-parity harnesses (audit/p1_parity_dump_test.go T15_OUT, maximization/parity_dump_test.go MAX_PARITY_OUT) whose only drivers were untracked .scratch scripts against the retired twin → deleted; (b) two findings placeholders asserting nothing; (c) FOUR docker e2e tiers behind WEBV2_DOCKER_TESTS=1 that no script set since they were written (cli/forkpoc/immunize/reproduction, ~35s, all green) → now the final stage of scripts/p2-docker-e2e.sh. Rule: a skip that nothing un-skips is a lie told in green; every env gate needs a gate script that sets it.
- [20260910-4ois] (tactic) web3sec-go probe-surface (F6/C2) closed 2026-09-10 in commit 870d42e: the reported "golden emits 0 rows for some probe axes" was TRUE but its cause was fixture SELECTION (recipe shipped accumulator/blind + assertion_strength/clean, never the buggy siblings), not missing assertions — measure a reported coverage gap before designing the fix. Where the detectors were already pinned byte-exact (probes/testdata/golden raw_<tree>_<probe>.json, 15x6=90 vectors), the real hole was the end-to-end path. Fix pattern that worked: (1) a state contract in scripts/check-golden.py (EXPECTED_PROBE_AXES: axis -> "rows"|"blind", both requiring sites>=1, `no-sites` refused) wired to the probes-list-all-json capture; (2) a Go floor test driving PRODUCTION opts (BuildSurfaceOpts+ProdProbeOpts) over the buggy corpus asserting >=1 row per registered axis + clean fixtures stay silent; (3) a Go test parsing the checker's table and comparing to RegisteredAxes() so a new probe must widen the gate. Note the golden NEEDS two blind axes for `probes blank` (blank keys), so the strict per-axis row floor can only live in Go — don't try to assert it in the golden. Prove every new refusal branch with a negative control (mutate the expectation/capture and watch it go red) before claiming the gate works.
- [20260910-zw22] (tactic) web3sec-go 2026-09-10 (commit b55ae3a): before building any item from docs/feedback-triage.md or a backlog doc, grep the code for it — Tier D was stale on BOTH items (D1 = the disposition linter had shipped as B4 incl. v1+v2 + --override-dismissal/--override-reason + brief/report surfacing; D2 = liveness impact had shipped as B1 with LIVENESS_LOSS in internal/capabilities + livenessImpact at materialization). The real remaining gap was not the feature but the PROOF and the record: the gate had unit tests but no end-to-end exercise, and its two human-facing outputs were unasserted. What closed it, and the pattern to reuse: (1) drive the refusal through the real CLI in scripts/golden-run.py (3 steps: refused / override-unreasoned / overridden), extracting the subject row dynamically from the surface via a state hook (`dr`) rather than hardcoding ids; (2) add declared output markers (expect_err/expect_out, parallel lists in spec.json, validated by check_steps) because an exit code never says WHY a step refused — my first attempt was caught by them (a probe row needs --anchor before the dismissal gate); (3) check the durable ARTIFACT too (report.md must carry the row as both a flagged dismissal and a logged override) — step captures only prove the CLI path; (4) if a policy decision is silent (the override was logged but never announced), make it loud via an out-param on the opts struct (AnsweredOpts.OverrideLogged) + a CLI line, and pin BOTH directions (override → notice set; refutation-backed → not set). Also: docs lived in assets/runbook/ are embedded — editing them fails TestAssetPackManifest until scripts/sync-asset-manifest.py runs, and walkthrough `L###` labels are decorative, unvalidated, and have been stale for a long time (shifting them keeps the error constant; fixing them is a recorded open item, not a bug).
- [20260910-u1ss] (tactic) web3sec-go: a policy gate that only ever refuses is untestable-as-a-wall — every new refusal rule needs BOTH a golden step proving the refusal (with markers asserting the why) and a step proving an accepting closure on the same fixture tier; the accept step is the load-bearing one. Also: prefer read-the-row identity checks over new index dependencies (surface rows already publish contract/consumer/base/asserter/concept_keys/forward/siblings), and keep one escape hatch per gate family, not one per rule.
- [20260910-c6n3] (fact) web3sec-go: campaign file listings must go through validation.ListPrefixed / ListSubPrefixed (ReadDir + prefix/suffix), never filepath.Glob — a Glob treats the whole path as a pattern, so a `--root` containing [ ] ? * silently reads the campaign as EMPTY (proved 2026-09-10: old binary `prioritize --root '/x/meta [2026]'` printed nothing and `dedup` reported untouched=0 where the fixed binary reports 1). Glob still legitimately used for pattern-y suffixes (planner/archive.go digit class, taxonomy config datasets).
- [20260910-7n2w] (tactic) web3sec-go CLI flag parsing: every value-consuming case must be `case a == "--x" && i+1 < len(args) && !looksLikeOption(args[i+1]):` (or the flagValue helper in cmd_exec.go). Without the guard `webv2 move <c> <f> DISPROVED --reason --actor` recorded the literal string "--actor" in the hash chain and exited 0. Standing guard: internal/cli TestNoUnguardedValueConsumption (source-level regex over the package) — keep it green when adding verbs.
- [20260910-oz7r] (correction) Clarify user intent before assuming action; they may need correct terms or guidance rather than execution of research.
- [20260910-j06l] (reusabletactic) Fact-check document claims by performing targeted web searches for specific events, incidents, or data points to verify accuracy.
- [20260910-3izk] (environmentfact) For frameworks focused on precision and calibration, research should emphasize actionable terms and validation over broad surveys or protocols.
- [20260910-a94z] (tactic) web3sec-go golden vectors (internal/orchestrator/testdata/oracles.json) cannot be hand-patched when a change moves event text: event_hash is sha256 over canonical JSON of the real finding ids, while the fixture stores F-<IDn> placeholders. Re-record the one affected snapshot from the Go twin via a throwaway test that replays the scenario through the same seams/root and dumps ids.place(DumpIndented(got)) + the placeholder-mapped campaign_state.json/events.jsonl, then prove the delta is only the expected lines (here: oracle 4/345, state 13/570, events 5/30). Note bash /tmp is per-call virtualized in this env — dump into the repo and delete afterwards.
- [20260911-s646] (fact) Any enum added to assets/schema/finding.schema.json MUST also be pinned in t14FindingLegend (internal/cli/cmd_ingest_test.go, walk-order) — TestIngestExampleLegendMatchesSchema fails the whole cli suite otherwise; gotcha hit by Wave G T5 (fixed 3004034).
- [20260911-ymi5] (fact) web3sec-go: adding assets/schema/<name>.schema.json is never a one-file change — also add the name to knownSchemas in internal/validation/schema.go AND update the pinned tuple literal in internal/validation/schema_test.go::TestUnknownSchema (and run TestAssetsEmbeddedCount), and any new artifact KIND must be added to the enum in assets/schema/campaign_state.schema.json or RegisterOrRefresh fails campaign_state validation.
- [20260911-y445] (correction) Avoid building parallel frameworks; instead, extend existing systems with modular backends to leverage proven tools and reduce duplication.
- [20260911-qx5o] (reusabletactic) When integrating security verification tools, prioritize zero-dependency options first (e.g., solc SMTChecker) to minimize setup complexity and dependencies.
- [20260911-3s62] (environmentfact) Complex tools like Certora consist of multiple chained components; understanding the full pipeline helps focus engineering efforts on the most tractable parts for integration.
- [20260911-7gnw] (fact) webv2 containment geometry (verified 2026-09-11): snapshot.source.root is ALWAYS <campaign>/snapshots/<id> (the immutable copy), so "campaign dir inside the pinned tree" is unsatisfiable on real data; when the campaign root does sit inside the pin target, stageTree silently prunes the leading path component (recorded in source.excluded, e.g. "ws") so the campaign's own files are never in the walked tree. To make the check decidable, the pin would have to record the target path.
- [20260911-ofts] (fact) Held-out eval gold must NEVER be embedded in the shipped binary (the audited agent runs it — leakage). Answer keys live outside the repo (targets/morph-gold-cases.json + .sha256 sidecar) and load at grading time via `webv2 scorecard/adjudicate <C> --gold FILE` (evalscore.LoadGoldPack, sha256-verified, provenance line printed). Join requires the campaign's --program to equal the case's program.program exactly-insensitively: the Morph campaign must be inited `--program Morph` (machine_scoring.program_key).
- [20260911-jwju] (fact) Adding a top-level campaign_state key touches 6 places: assets/schema/campaign_state.schema.json, internal/state/campaign.go init list, campaign_test.go wantKeys, PLUS three byte-compared fixtures that embed state docs as ESCAPED JSON strings — internal/floors/testdata/golden/campaign_state.json (1), internal/orchestrator/testdata/oracles.json (7), internal/pipeline/testdata/scenarios.json (11). Patch the escaped form `\n \"key\": []\n}` — rg -c on quoted keys fails on these files; the embedded docs end at the last key + `}`.
- [20260911-33hn] (correction) Don't pin evolving counts in gate/release scripts: scripts/release.sh grepped the literal "embedded assets ok: 28 schemas" and failed at 32 even though selftest printed ALL PASS — assert the line, not the number (fixed 20260911). Same class: EXPECTED_SECTIONS / pinned command counts only survive when presence-gating keeps new output absent. Also: re-measure classifier-derived numbers after any classifier rule change (srcclass implementation went 62→40 files when the interface rule became per-segment); never reuse earlier measurements in specs.
- [20260911-evpp] (fact) Temporal rule (fixed 20260911): evalstore temporal comparators must carry REAL dataset provenance — source.dataset=="manual" rows (build-stamped planted fixtures) can be excluded but can never cause an exclusion; missing/malformed source stays a comparator (fail-closed); near-dup stays provenance-blind. Also: evalstore.LoadCases does NOT check duplicate case_ids (only VerifyEvalStore does) and backtest keys exclusions by case_id — a gold pack whose ids collide with the shipped counter (CASE-000000000001..13 hex = 19 rows) silently drops 2x rows in a merged store; the Morph gold pack uses provenance-derived ids CASE-2024a0923001/02 for this reason.
- [20260911-i3nw] (fact) web3sec-go discovery budget (post 2026-09-11 reform, commit b95ec567): the slot is charged ONCE per finding at its first rise above E0 — internal/findings/slot.go ConsumeSlotOnce (idempotent via the finding's optional discovery_slot_consumed flag, NOT the counter), called from AddEvidence (risesAboveBaseline), IngestHypothesis (pre-loaded above-E0 payload), and transition() (status floor index via STATUS_FLOOR, before applyStatus). Bare HYPOTHESIS ingest is free; keys/ceiling/refusal text unchanged. Fixture gotcha: internal/orchestrator/testdata/oracles.json embeds 7 campaign-state docs as ESCAPED JSON strings; only that file moved (3 counters 9→0) — floors/pipeline fixtures were already 0. Budget display wording is now "risen so far".
- [20260912-a63m] (tactic) Verify byte-pinned specs by writing a scratch program to extract and compare expected strings
- [20260912-mcroots] (fact) MiniCertora Python package root is /home/xand/Projects/minicertora/minicertora/ (cli.py + pyproject there; nested minicertora/ is a stub pkg). Must run from that root with .venv/bin/python (sys.path CWD-first trap); --solc-path wants an absolute compiler path for reproducible runs; z3 is the Python z3-solver binding (5.1.0.0), cvc5 optional via MINICERTORA_CVC5.
- [20260912-gocache] (fact) This sandbox's default ~/.cache/go-build is read-only: run every Go command as GOCACHE=$PWD/.gocache (gitignored). Bare `go test`/`go vet` fail with cache-write errors, not code errors.
- [20260912-displaynil] (correction) Wiring a CLI presentation onto a pure mapper must enumerate the output COMBINATIONS, not just verdicts: `verify --harness-result` crashed on *boundedK for a legal proved-bounded-without-bounds line (MapMinicertora's documented shape). Law: presentation branches may re-read the proof sidecar (proofLoopBoundText) but never dereference optional pointers unguarded; pin one CLI row per display mode.
- [20260912-proofcontract] (fact) The minicertora proof sidecar's contract is the KEY SET + ORDER (12 keys, fixed order ending ghosts, invariant, calls — mcProof), values admitted VERBATIM (schema unconstrained-by-design on scalars; arrays/ghosts/invariant/calls may be null (invariant object-or-null, calls array-or-null, both verbatim); bounds keeps its three keys, additionalProperties:false). Verdict authority is the rung; the sidecar is copyable evidence. Rung display k precedence: bounded_k pointer -> proof.bounds.loop_bound Int/Big (IntText) -> k-less line.
- [20260912-goldenverdict] (fact) scripts/golden.sh's success verdict since the Python-twin retirement is "GOLDEN GREEN: Go run validates (exit codes, tree + event chain, audit surface)" — there is no byte-MATCH vs a twin anymore, and check-golden.py no longer byte-diffs two implementations. Single-instance rule still holds (ps check first).
- [20260912-dywf] (correction) Always cross-check documentation counts against source of truth (e.g., REASON_CODES from runner.py) before implementing to avoid mismatched numbers
- [20260912-jydg] (tactic) Check for related code patterns (e.g., 'unbound', 'REASON_CODES') across all files to ensure fix consistency.
- [20260912-0h9q] (fact) web3sec-go: changing assets/prompts/47_proposer_system.md breaks two independent corpus gates — (a) Go tests internal/sft TestSFTSeedStoreLintsCleanAndShape/TestSFTSeedExamplesPassFullLint require sft/examples.json messages[0] to be byte-identical to the prompt (and internal/taxonomy/testdata/seed/examples.json is its byte-mirror), resync via escaped-text substitution, not reformat; (b) the FROZEN golden fixture scripts/golden/p4/sft/{examples,lint-pass,lint-dedup,lint-reject}.json (commit 91336adc, built from the retired ../web3sec-final store) also embeds the prompt, so golden step 175 sft-lint-pass goes RED with "system-prompt drift" — regenerating via scripts/golden/p4/build.py cannot fix it (it reads the old twin store) and task notes forbid touching fixtures, so such a change must be reported BLOCKED for a controller ruling. Resolved 2026-09-12 by ruling: the 7 embedded copies are resynced in place by exact JSON-escaped-text substitution (no re-dump/reformat), go test stays green and golden step 175 goes GREEN; build.py is documented as no longer the owner of those copies.
- [20260912-2le4] (tactic) When inspecting JSON schemas, use Python to walk the schema structure programmatically rather than manual grepping
- [20260912-e98s] (fact) minicertora tool output has two line shapes: verdict lines are the 23-key build_report object (+ "invariant" appended last = 24 keys on invariant lines, via cli.py::_invariant_report), while refusals are rule-LESS envelopes — _emit_abort/_emit_refusal print {"verdict","reason","details"} (3 keys) and _emit_undecided prints {"verdict","rule","reason","details"} (4 keys). Consequences: any consumer joining on rule/contract silently drops document-level refusals, and packed-storage-rejected is reason "rejected-feature" with details exactly "packed-storage:Packed.b" (whole-target abort, exit 2), never unsupported-storage-layout in a full report line. Full-report UNKNOWN lines carry only invariant-* reasons (invariant-uninitialized / invariant-unchecked-functions) and tool-error from _refuted_assert. Also: PROVEN lines always have params={}, initial_storage={}, final_storage={} (no SAT model; only `calls` is re-extracted for queries/vacuous).
- [20260912-0h9q] (fact) web3sec-go: changing assets/prompts/47_proposer_system.md breaks two independent corpus gates — (a) Go tests internal/sft TestSFTSeedStoreLintsCleanAndShape/TestSFTSeedExamplesPassFullLint require sft/examples.json messages[0] to be byte-identical to the prompt (and internal/taxonomy/testdata/seed/examples.json is its byte-mirror), resync via escaped-text substitution, not reformat; (b) the FROZEN golden fixture scripts/golden/p4/sft/{examples,lint-pass,lint-dedup,lint-reject}.json (commit 91336adc, built from the retired ../web3sec-final store) also embeds the prompt, so golden step 175 sft-lint-pass goes RED with "system-prompt drift" — regenerating via scripts/golden/p4/build.py cannot fix it (it reads the old twin store) and task notes forbid touching fixtures, so such a change must be reported BLOCKED for a controller ruling. Resolved 2026-09-12 by ruling: the embedded copies (8: four files under scripts/golden/p4/sft/ plus scripts/golden/sft-example.json — golden.sh does NOT read the latter, runbook-walkthrough.sh does, so run BOTH after a prompt edit) are resynced in place by exact JSON-escaped-text substitution (no re-dump/reformat), go test stays green and golden step 175 goes GREEN; build.py is documented as no longer the owner of those copies.
- [20260912-64y6] (correction) Test only the specific models the user mentioned; do not expand to unrequested expensive models.
- [20260912-zo6s] (reusabletactic) When probing third-party APIs for parameter support, use minimal model sets and lightweight requests to avoid unnecessary costs.
- [20260912-qbhx] (correction) Kill and restart test scripts immediately when user restricts scope, rather than letting oversized probes run to completion.
- [20260912-mn5u] (lesson) When modifying a prompt or asset that is embedded in multiple other files (like golden JSONs), always resync all downstream copies; a single sweep can miss some, causing test failures.
- [20260912-7w5z] (lesson) For complex documentation tasks, run a verification battery early and in the background to catch regressions before finalizing changes.
- [20260912-6h9s] (lesson) Maintain a running precedent list in docs (like IMPROVEMENTS.md) for recurring patterns (e.g., corpus-resync) to prevent repeated mistakes and guide future maintenance.
- [20260912-7ly8] (correction) When a session restart fails, check for concurrent-resume corruption in session logs by analyzing sequence numbers for overlapping runs
- [20260912-gm60] (tactic) Use repair_session_log tool with scan, analyze, then repair actions for corrupted DSH sessions
- [20260912-576y] (environment) DSH sessions store logs in ~/.dsh/sessions/{workspace}/{session-id}/ directory structure
- [20260912-kebu] (lesson) Always match inner reason text before separator when classifying decorated runtime floors to avoid misrouting to wrong disposition class.
- [20260912-3v9c] (lesson) Stage only explicitly listed files, never git add -A, to avoid committing unintended changes like uncommitted LEARNINGS.md lines.
- [20260912-xrav] (lesson) Use grep/rg with specific file patterns and offsets to efficiently locate audit pins, doc anchors, and sweep references in large codebases.
- [20260912-oqw6] (tactic) When reviewing code changes, independently re-run tests and byte-compare vendored files against upstream to verify claims
- [20260912-87e0] (tactic) Extract and diff enum/code sets from both source (Python) and target (Go) to ensure complete coverage
- [20260912-6cqe] (correction) Always fetch background job outputs explicitly rather than assuming they complete before review
- [20260912-8bjs] (fact) web3sec-go: `.superpowers/sdd/.gitignore` is `*` — the whole SDD ledger (task-N briefs/reports, plan files) is UNTRACKED, so ledger edits (e.g. appending a Final-review section to task-6-report.md) live only in the working tree; never `git add -f` them and never expect them in a wave commit (verified 2026-09-12: `git ls-files --error-unmatch .superpowers/.../task-6-report.md` exits 1).
- [20260912-ag8y] (environment) Do not use /tmp for multi-step scratch state — it is per-call ephemeral; use workspace .scratch/ instead.
- [20260912-005j] (tactic) Re-verify fix claims with independent live probes on fixture copies, never trusting reports or mutating golden fixtures.
- [20260912-wxiz] (tactic) Validate self-test pins with tamper/mutation probes (neutralize fix, expect nonzero exit) plus git status and test gates.
- [20260912-u5lh] (tactic) Read binding spec Global Constraints + Task lines in-file plus scaffold-bound rail arm and harness.Validate before editing.
- [20260912-t1ic] (tactic) Use TDD RED-GREEN for validation rails: add failing test rows first, watch them fail, then minimal implementation.
- [20260912-bk43] (environment) Go repo requires GOCACHE=$PWD/.gocache for go test/vet and golden gate via scripts/golden.sh only after golden-idle pgrep check.
- [20260912-gnvg] (fact) minicertora witness value channel (verified 2026-09-12, cli.py L1008-1033 + solve/backend.py:39): env["msg.value"] is always `str(model.eval_bitvec(expr))` — a DECIMAL wei string, "0" default, JSON null when the model never evaluated it (vals keeps None); `_pretty` only renders args/overrides and is also str(); a repo-wide rg for ether/gwei/1e18 in minicertora finds only "whether", so no pretty/unit form is reachable. The sequence_poc step `value` key (L-defer T3, commit 01e91ed9) is optional with pattern ^(0x[0-9a-fA-F]{1,64}|[0-9]+)$; harness.BridgeSequence carries it verbatim, omits it on zero/absent, refuses anything else with `unbridgable step: call <n> value <raw> not a wei literal` (raw Python-repr'd), and internal/sequencepoc driver.go threads it as `cast send --value <literal>`; real `cast` accepts --value AFTER positional args.
- [20260913-k4dq] (environment) /tmp does not persist across bash tool calls — stage scratch verification files under workspace .scratch instead
- [20260913-j99t] (tactic) Re-execute implementer verification battery independently (self-test, RED vs pinned baseline, tamper copy, mutations) rather than trusting report leftovers
- [20260913-5pzm] (tactic) Separate stdout/stderr streams and ensure synthetic inputs satisfy shape/envelope laws when building collision or mutation cases
- [20260913-20lw] (tactic) web3sec-go close-outs: when a controller ruling asserts a mechanism "fired live" (e.g. wave M collision law in run 2), re-run the instrument on the COMMITTED data + committed config before encoding it — run-2's map lacked the LegacyVaultT key so tie_collision returned None (9 tied, 0 unjoined, no exclusion line) and the prose claim was false; encode the verified truth and flag the contradiction.
- [20260913-qpgu] (correction) Disable HTML escaping in JSON encoder when oracles contain placeholders like <ID1> to avoid \u003c mismatches
- [20260913-nqbt] (tactic) Regenerate golden oracles by replaying scenarios with the test harness instead of hand-patching embedded leaves that cascade hashes
- [20260913-uj1u] (tactic) Reset oracles.json via git checkout before regen and use removable zz_*_test.go one-shot tools rather than stacking manual edits
- [20260913-256z] (tactic) Verify every code-review suspicion by execution: build with GOCACHE=$PWD/.gocache and run probe binaries plus go test ./... instead of trusting code reads.
- [20260913-jv2u] (tactic) When asked to verify prior fixes, diff the base commit range (e.g. 283528c3^..HEAD) and re-check each claim against current code and docs.
- [20260913-4kyz] (correction) Prefer the file-read tool over fragile shell pipelines; an rg flag typo mangled output and required a proper read of slot.go.
- [20260913-jwpu] (tactic) Use single IsTerminal predicate instead of hand-copied DISPROVED/REJECTED/EXPIRED lists.
- [20260913-f38o] (tactic) Verify trust-sensitive gate decisions with both forward and reverse hand-edit scenarios.
- [20260913-4n6r] (environment) Set GOCACHE to repo-local writable dir (e.g. $PWD/.gocache) before go build in sandbox.
- [20260914-ddbn] (environment) Set GOCACHE to workspace .gocache for all go build/test to avoid cache permission failures.
- [20260914-e5ev] (tactic) Build CLI into .scratch and drive init/snap/audit/strip/re-pin sequences to verify projection and heal paths.
- [20260914-jxrl] (correction) Treat Python audit.py as twin law: state row without ledger snapshot.pinned event is red unconditionally.
- [20260914-4ai8] (correction) Never run scripts/runbook-walkthrough.sh, scripts/golden.sh, or git commands in web3sec-go audit tasks.
- [20260914-8h2t] (environment) Build/test Go with GOCACHE=$PWD/.gocache (e.g. GOCACHE=$PWD/.gocache go test ./internal/cli/ -count=1).
- [20260914-s8xz] (tactic) For docs-only fixes verify no .go files touched to justify skipping gofmt and keep scope to docs.
- [20260914-iqve] (fact) web3sec-go harness rung decision is ONE function: harness.DecideBound(kind, inv, raw, rec, scaffold, timedOut, k, exitStatus, ruleName) in internal/harness/rederive.go owns the recorded-hash arm + Validate re-render + unbound suffix + kind dispatch; cli.harnessMapBound and audit section 11's recheckExecEvidence/recheckMapRunEvidence/recheckInconclusive all call it. Never call MapMinicertoraInvoc/MapRun directly from the audit (r28b F3: that reproduced only the last step, so claim drift and foreign recorded hashes audited green). Exit status has one reader, harness.RecordExitStatus (absent/null/big -> -2) + RecordTimedOut; nil scaffold = audit's "bytes unavailable" (hash arms refuse, unbound arm proceeds). Section-11 fixture campaigns have NO scaffold artifact, so audits must tolerate that arm.
- [20260915-uij7] (tactic) Re-derive EVERY bind decision through the SAME code path field-by-field with fixtures built via the real binary (init/model/scaffold/exec).
- [20260915-p3cs] (tactic) Treat twin repo as authoritative for CLI semantics and probe the real click command object instead of trusting docs.
- [20260915-8qof] (environment) Twin at /home/xand/Projects/miniprover is read-only authoritative; DSH policy is danger-full-access with approvals disabled.
- [20260915-hh6j] (tactic) Probe real tool binaries (forge/halmos/minicertora/miniprover) and twin repo to pin gate behavior authoritatively before fixing
- [20260915-jgqu] (tactic) Follow existing bind-vs-audit pair test as pattern and write failing cli plus section-level repro tests first
- [20260915-6rmq] (environment) Use rg instead of grep and run go test with GOCACHE=$PWD/.gocache
- [20260915-tue3] (correction) Touch only assigned files when another agent edits same package concurrently; report breakage elsewhere, never fix it.
- [20260915-teud] (environment) Use GOCACHE=$PWD/.gocache, gofmt touched files, search with rg, and never run a webv2 verb from repo dir without --root under /tmp/<own-dir>.
- [20260915-syh1] (tactic) Write a failing pin test first (observe FAIL), then fix and re-run for PASS to lock F1/F3 rail behavior.
- [20260915-rbja] (correction) Build with GOCACHE=$PWD/.gocache and keep all --root test data under /tmp/... only, never modifying repo files or running repo scripts.
- [20260915-obdd] (tactic) Audit sandbox exec timeouts with TERM-ignoring children, detached escapees, and container processes that outlive killed records.
- [20260915-rf62] (tactic) Probe workdir honesty and record fidelity via symlink/deleted workdirs, missing commands, output truncation, and exit-status checks.
- [20260915-qsv6] (correction) Search with rg shell command, never grep per repo instructions.
- [20260915-ir96] (correction) Touch only assigned files with concurrent editors; report breakage elsewhere, never fix it.
- [20260915-pkof] (tactic) Docker-dependent tests must SKIP cleanly without docker but verify real container behavior with live docker/binary repro.
- [20260915-lr02] (correction) Touch ONLY assigned files — other agents edit disjoint files concurrently
- [20260915-qfv4] (correction) Keep scratch under /tmp/<yourowndir> and delete when done — box is tight on disk
- [20260915-4c9g] (correction) Do not chase MiniCertora findings or docs — integration is under construction, report and move on
- [20260915-s606] (tactic) webv2 unwind audits: drive the honest refusal by growing the ledger with `cost <C> --kind model --amount 1` (3×), then cutting events.jsonl to a prefix; then run a GHOST-WRITE SWEEP — for each CLI verb, snapshot the whole campaign tree (sha per file, normalize `updated_at` inside campaign_state.json), run the verb, and diff: any file/state field that changed while the log printed "the state projection mirrors ... the write is refused" is an unmigrated save-then-log site. This found r41's `recall` (findings/memory.go RecordMemoryCheck -> gate's memory-check clause satisfied with 0 events) and `ladder reopen`/`set-maximal` (missing restoreLadderPair on the SaveFinding arm).
- [20260915-vz3b] (correction) Never delete or modify the isolated GIT worktree; do all mutable work in scratch dir and set GOCACHE inside worktree.
- [20260915-eofg] (correction) Do not request sandbox escalation via sandbox_permissions when approval prompts are disabled — such actions are auto-rejected.
- [20260915-91bw] (tactic) Test sweep/rotation doors with a grow-ledger then truncate-events.jsonl refusal harness checked by byte-identity and post-heal retry.
- [20260915-6r18] (correction) Preserve the isolated audit worktree and its .gocache; build with GOCACHE=/tmp/w3-r44/.gocache and do all mutable probes in scratch under /tmp/c44root.
- [20260915-gjij] (tactic) After a helper refactor, grep for direct os.ReadDir/filepath.Glob bypasses and probe each with chmod-000, file-as-dir, and dangling-symlink to expose refusal-folded-as-absence bugs.
- [20260915-uczx] (correction) Measure CLI exit codes via PIPESTATUS or without pipes since piping through tail/head masks failures.
- [20260915-gyq4] (correction) Propagate store-list errors (AllExecs/ListCampaigns) instead of silently proceeding with partial data on err==nil.
- [20260915-miki] (tactic) Keep one canonical list implementation and delegate wrappers to it (e.g., sandbox.AllExecs -> state.AllExecs).
- [20260915-6d1u] (environment) Preserve import direction sandbox->state; state and validation must not import sandbox to avoid cycles.
- [20260915-d9gi] (correction) Do not pipe probed CLI through tail/head when checking exit codes; capture output first then status or use PIPESTATUS.
- [20260915-drmy] (correction) Verify constructed campaign/snapshot paths exist with ls before probing to avoid path-variable bugs.
- [20260915-ccyy] (environment) Never delete/modify isolated /tmp/w3-r45 worktree or its .gocache; build with GOCACHE=/tmp/w3-r45/.gocache and use separate scratch dir.
- [20260916-ne2g] (fact) MiniProver (/home/xand/Projects/miniprover, branch feat/implement-plan) now has the minicertora v0.4 contract implemented and committed on 2026-09-16: f82316f (code+tests, 325 passed/mypy strict clean), 95d7724 (evidence runs counter/v04 + counter/v04-relative-root-failure), 9abd38b (docs). Consequences for web3sec-go: docs/MINIPROVER_INTEGRATION.md §7 still says "minicertora v0.4 (R1-R15) is unshipped" — STALE, re-check §7 and re-run tools/minicertora_conformance.py before trusting it; the v04 run sets capabilities_missing EMPTY (our doc's "degraded run" wording is about v0.3 runs only). Report contract unchanged: report.json schema_version is still "1.0", verifier block = schema 0.2.0/tool 0.1.0/spec v0.3/solc 0.8.36, so our ReportSchemaMajor gate and _PROVENANCE_FIELDS still speak. Root-cause bug worth knowing on our side: every minicertora invocation runs with cwd=<temp scratch>, so any path passed to it must be absolute — a relative --project-root made the first v0.4 run publish nothing (all 8 properties NOT_ATTEMPTED, 0 decisions).
- [20260916-izqz] (correction) When behavior change ripples to pinned goldens, regenerate then diff to prove only intended keys changed.
- [20260916-bsyq] (correction) Isolate out-of-scope suite failures to their owning task instead of expanding current task scope.
- [20260916-wz5e] (environment) Run Go commands with GOCACHE=/tmp/gocache-webv2 and stash temp outputs under .scratch/ to avoid repo pollution.
- [20260916-lr6g] (tactic) Create the failing test first to confirm missing API/behavior before implementing the fix.
- [20260916-82m2] (tactic) Run targeted tests, then broader package suites plus build and vet, and check goldens/schemas when output shape changes.
- [20260916-h16r] (tactic) Verify final diff is scoped to intended files with git status and git diff --stat.
- [20260917-ndgr] (tactic) web3sec-go tool docs (docs/MINI*_INTEGRATION.md) drift whenever the twin repos move: always re-measure at a named HEAD sha (minicertora be14d3a, miniprover 9c997b3 as of 2026-09-17) and re-derive each claim from a real run — conformance probe, `--collect-only` for test counts, `guide --env` for env vars (which does NOT print MINIPROVER_MAX_TOKENS*), a live corpus line for the report key set, and the prover's own `load_invariants` for the --invariants ledger shape.
- [20260917-j4tm] (correction) Do not pipe `go test` to `tail` for verdicts — it masks failures; save full log and scan for FAIL plus exit code.
- [20260917-07kh] (tactic) Verify additivity/no-breakage claims by exhaustively grepping all readers of the changed field, not trusting the fix report.
- [20260917-4h4p] (tactic) For scoped fix re-reviews verdict each prior finding ADDRESSED/NOT ADDRESSED and flag only new breakage inside the fix diff range, leaving tracked tree clean.
- [20260918-udvs] (correction) Golden oracle re-record harnesses must replay every scenario step exactly like the real test, not only asserted steps, or state diverges and oracles corrupt.
- [20260918-h41l] (tactic) Restore golden files with git checkout before re-recording so the diff reflects only the intended behavior change.
- [20260918-iicm] (tactic) Delete temporary zz_* re-record test files after regeneration to avoid shipping one-off tooling.
- [20260918-sxjf] (fact) web3sec-go worktree gates: the briefed `GOCACHE=$PWD/.scratch/gocache go test …` fails with "[setup failed] … read-only file system" (golang.org/x/text v0.39.0 missing from the read-only /home/xand/go/pkg/mod) — always run gates as `GOCACHE=$PWD/.scratch/gocache GOFLAGS=-mod=mod GOPATH=$PWD/.scratch/gomod GOMODCACHE=$PWD/.scratch/gomod/pkg/mod go test …` (repo-cache convention from task-9-report.md) and note the substitution in the report.
- [20260918-v2gw] (tactic) When a fix is mirrored in multiple packages, compare the blocks byte-for-byte to ensure all copies are patched
- [20260918-a6bo] (tactic) Delete temporary probe test files and verify git status clean after read-only reviews
- [20260918-gwlv] (tactic) For regex boundary and precedence fixes, run adversarial cases plus full test suite to confirm no regressions
- [20260918-001d] (fact) web3sec-go gate scripts force the repo Go cache with PLAIN exports (`export GOCACHE="$ROOT/.scratch/gocache"` / GOPATH / GOMODCACHE, GOFLAGS=-mod=mod) — never `${VAR:-…}`: the harness exports GOPATH=$HOME/go (read-only under the sandbox) and the default form loses to it, so builds/scans die on "read-only file system" download locks. Mutation-check harness for a script's own export block: `.scratch/sdd/cache-export-probe.sh <script>` (evals the block with the caller env, then builds).
- [20260918-kx3h] (fact) web3sec-go coverage semantics gotcha: internal/coverage.RefreshGaps counts trajectory KEYS (`len(trajectory_counts) < 2` => thin-trajectory), not the sum of the counts; planner.coverageSwept (`status` not ""/"unknown" and >=1 key) is a deliberate duplicate — importing internal/coverage from planner closes a test-binary cycle (coverage -> audit/sections -> completion -> planner), so the two are pinned together by internal/planner/task12_coverage_parity_test.go (known divergences: swept-with-zero-counts, excluded).
- [20260918-7wau] (fact) web3sec-go audit surface: 16 registered sections = 14 unconditional (event_log..unpriceable) + 2 presence-gated appended last in register.go order (eval, then price_table; each returns sections.ErrSkip and audit.go:88 omits it). verify-full.sh's shared p2_sections_ok (steps 9/11/12) accepts "14 base in registration order + any documented gated tail", mirroring check-golden.py's check_audit (golden step 164=15, 194=14); step 12 prices before auditing so it renders 15. Green trail log is tracked at docs/sdd/verify-full-final.log (.scratch copy is gitignored).
- [20260918-hn40] (tactic) web3sec-go: editing assets/runbook/RUNBOOK.md (or any embedded asset pack) requires `python3 scripts/sync-asset-manifest.py` (then `--check`) — verify-full step 6 is `go test ./assets -run TestAssetPackManifest -count=1` and there is no Makefile. Also: gate "N packages ok" figures counted with `rg -c '^ok'` on a CONCATENATED multi-gate log are artifacts (golden.sh and security-check-test.sh emit their own `ok` lines) — the repo has 73 Go packages (71 with tests, 2 `[no test files]`); count from a single-command capture.
- [20260918-14z9] (correction) Probes asserting absence of a side effect must confirm they read the correct store (campaign state vs link registry) before trusting a pass.
- [20260918-ymqg] (tactic) When reviewing a validation gate, trace and live-probe resolver side effects that run before the gate to catch refuse-but-still-mints behavior.
- [20260918-4j2e] (tactic) Prove shared-helper non-perturbation by showing the diff is pure insertion with zero deletions plus running focused matcher and CLI tests.
- [20260918-l11v] (fact) web3sec-go: DefaultPlanFromModel/bootstrapOpenQuestions live in internal/planner/plan.go (not internal/orchestrator/plan.go — plan briefs keep mis-pathing this); minting a new plan-priority id family (e.g. LC-%03d) also requires widening assets/schema/campaign_plan.schema.json's priority id pattern AND re-running python3 scripts/sync-asset-manifest.py, because assets/manifest_test.go verifies the embedded-schema hashes. plan.Validate runs inside DefaultPlanFromModel, so a non-Q id fails the whole Plan verb.
- [20260918-pdcv] (tactic) In this worktree, bash `rg`/`grep` stdout sometimes mangles identifiers (e.g. cap→n, reachability→n, fork-runner→n) — never trust exact strings from rg output; re-read the file with the read tool or ipython for verbatim text, and use rg only for locating line numbers.
- [20260918-iz44] (environment) Go builds/tests need writable caches: export GOCACHE=/tmp/... GOMODCACHE=/tmp/... with GOTOOLCHAIN=local GOSUMDB=off when workspace is read-only
- [20260918-qhgp] (tactic) Verify plan/report premises directly in source with rg/grep for exported symbols before ruling compliance
- [20260918-odq5] (tactic) For independent reviews confirm scope with git log and diff stat then rerun relevant package tests and golden gate
- [20260918-6p9j] (tactic) Verify protocol doc code quotes verbatim against pinned-commit source with sed/rg before marking spec compliant.
- [20260918-qg3q] (tactic) Reproduce a doc's minimal example in an isolated scratch campaign using the pinned binary to prove it runs.
- [20260918-p4oh] (tactic) Trace schema-enforcement claims end-to-end from CLI through LoadModel to Validate instead of trusting doc citations.
- [20260918-paqu] (tactic) Final whole-plan gates must re-verify code at task seams with git diff and run full test gates instead of trusting per-task review files alone.
- [20260918-4uye] (correction) Retrieve background bash results with job_output(job_id) directly; do not probe via ipython dummy or tool_search/tool_load.
- [20260918-1pj7] (tactic) Write review verdicts as new untracked files and leave tracked source unchanged during read-only gate reviews.
- [20260918-7hx9] (correction) Treat scratch/work dirs as output-only; production and test code must not read them as input truth.
- [20260918-69s4] (tactic) Before bulk deletion, check recent mtimes for live agents and grep for runtime string literals referencing the paths.
- [20260918-oflv] (tactic) Prove cleanup was safe by running the full build/test gate cold after the purge.
- [20260918-8ggf] (tactic) web3sec-go worktree refactors: the worktree copy can differ from the main checkout (aislop-ignore comment markers are only on the aislop-fixes lineage) — always read the file inside the worktree before editing; and batched multi-edit splits of one function leave transient syntax errors between edits, which is expected, just finish all chunks.
- [20260918-zgyz] (fact) aislop's complexity rules count CODE lines, not raw lines: `function-too-long` (max 80) and `file-too-large` (max 600) leave comment-heavy monoliths unreported — e.g. briefing.BuildBrief (438 raw lines), report.findingSection (435), briefing.AttentionLedger (283), coverage.go (1009 raw) were never flagged. Measure raw lines yourself (`rg -l "^func " --glob "*.go"` + brace-walk to col-0 `}`) or the biggest functions/files survive a "clean" scan. Test files are excluded from file-too-large. Suppression directive: `// aislop-ignore-next-line <rule> — reason`.
- [20260918-3p5v] (fact) webv2 run halts: needs_model → exit 3 (cmd_run.go:122), scheduler "halted" → exit 2 (cmd_run.go:125), propagated via t14Dispatch (cmd_scope.go:78-86); mapRunError (cli.go:344) is only the generic error path (exit 1), never on halts. Run go tests with GOCACHE=/tmp/gocache (default cache is read-only in sandbox).
- [20260918-76zp] (tactic) web3sec-go: assets/testdata/asset_manifest.json byte-gates every pack file per commit — after multi-package edits, regenerate the manifest per package commit (restore pre-edit asset bytes, sync, commit) so no intermediate commit fails TestAssetPackManifest (bisect gate).
- [20260918-xmxl] (tactic) web3sec-go Phase-C waves run several agents concurrently inside the SAME Go package (internal/cli) and cross-package (internal/findings): `go build`/`go test` flake with "undefined: <fn>" from another worker's half-written file. Don't debug it — poll `go vet ./<pkg>/` in a retry loop (20s x 60) in a background job, run the scoped tests on the first clean pass, and re-verify `go build ./...` immediately before handoff; note the flake in the report.
- [20260918-soyx] (tactic) Mirror existing artifact shapes from replay testdata and reuse state/validation helpers instead of inventing new schemas
- [20260918-3h2k] (environment) Run Go with GOCACHE=$PWD/.scratch/gocache GOFLAGS=-mod=mod and verify with go vet/build ./internal/cli/
- [20260918-u1v8] (correction) Locate CLI test harness and helpers with rg before writing regression tests
- [20260918-66vt] (fact) web3sec-go: `go test ./internal/findings/` compiles the EXTERNAL findings_test package, which imports internal/cli (gate_remediation_guard_test.go) — so findings tests cannot build while internal/cli is broken. Phase-C packages run concurrently in this repo, so the tree is intermittently unbuildable; poll `go build ./...` and don't edit the other package's in-flight files. Also: findings → planner is an IMPORT CYCLE (planner → bounty → findings), so planner predicates/constants must be copied into findings with a pin test (precedent: anchorlink's copy of planner.ProbeRowDispositioned).
- [20260918-zqf1] (environment) Run Go commands with GOCACHE=$PWD/.scratch/gocache and GOFLAGS=-mod=mod or build/test fails or pollutes cache
- [20260918-gx6w] (correction) Run gofmt -l/w and go build ./... before go test to catch formatting and build breaks early
- [20260918-cfih] (correction) Clean up temporary demo workspaces under .scratch and never modify out-of-scope assets or consumed-only packages
- [20260919-rmb8] (correction) Restrict edits to internal/briefing/*_test.go only and copy sibling floorEvidence patterns instead of inventing new fixtures
- [20260919-vbdk] (tactic) Fix shared transition funnels like confirmSimple to inject E2 floor evidence before POSSIBLE to cover many failures at once
- [20260919-5jdn] (environment) Verify briefing fixtures with GOCACHE=/tmp/gocache go test -p=1 -count=1 ./internal/briefing plus gofmt check
- [20260919-07wr] (tactic) web3sec-go R3-3 floor fixtures: when a fixture must reach POSSIBLE (E2 floor) on a finding tied to an UNVERIFIED model invariant, the floor item cannot be added after the citation — the rise guardrail refuses the level-raising AddEvidence it needs. Fix: earn the floor BEFORE the citation is attached (the package's findingWithInvariant already ingests first and cites after), via the ordinary AddEvidence path; keep the post-add finding value (SaveFinding on the pre-add copy clobbers it). A test measuring a "sub-E4 rise" refusal then has to measure E3, not E2 (the fixture is already E2).
