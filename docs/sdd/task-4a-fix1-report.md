# Task 4A — fix round 1 report

**STATUS: DONE** (no push, no merge, no delegation)

- **Worktree:** `/home/xand/Projects/dsh-plugins/websec2/web3sec-go/.worktrees/production-readiness`
- **BASE:** `521eecfd`
- **Commit:** `f13ae1df` — `test(invariants): re-pin twin fixtures for attestation field (additive)`
- **Working tree after commit:** clean (`git status --short` empty)
- **Test line:** `go test ./internal/invariants ./internal/cli -count=1` → **exit 0**
  (`ok websec/internal/invariants 0.229s`, `ok websec/internal/cli 28.801s`)
- **Byte delta:** confirmed to be **exactly the one added key** —
  `verification_method: "operator-attestation"` — in every file, with no other
  value, key or type change. Evidence below.

## 1. Additivity established before touching a byte

Commit `521eecfd` writes `verification_method: "operator-attestation"` on the
registry entry (`invariants.VerifyInvariantStatement`) and on the
`invariant.verified` event's `data`. Both readers were read first:

| reader | keys it reads | reads the new key? |
|---|---|---|
| `internal/invariants/guard.go` (`IsVerified`, `AssertInvariantsVerified`) | `status`, `verified_by`, `contradiction`, `source`; event `data.artifact` / `data.evidence` | **no** |
| `internal/audit/sections/invariantverification.go` (§11) | `status`, `verified_by`, `verification.harness`, `IsVerified` | **no** |

So no verdict moves: the key is pure provenance. Old readers ignore an unknown
JSON key, which is what makes the re-pin safe.

## 2. The byte-delta safety check (the core requirement)

The two tests write into `t.TempDir()` and compare bytes without dumping them,
so a temporary in-package replay harness (`internal/invariants/zz_repin_capture_test.go`,
**deleted before commit** — it never appears in `f13ae1df`) reproduced the exact
write paths of both tests and copied the produced bytes to
`.scratch/sdd/task-4a-logs/actual/`. The pinned bytes were never modified before
the diff was read.

Independent Python deep-diff (not the Go test's own assertion), full transcript
in `.scratch/sdd/task-4a-logs/fix1-delta-check.log`:

| file | adds | unsanctioned deltas |
|---|---|---|
| `testdata/scenario_links.json` | 2 | **0** |
| `testdata/guard_links.json` | 1 | **0** |
| `testdata/scenario_steps.json` | 14 | **0** |
| `testdata/scenario_events.jsonl` | 2 | **0** |
| `testdata/guard_events.jsonl` | 1 | **0** |

Every add is exactly `/…/verification_method = "operator-attestation"`. The
paired `KEYORDER` delta is the derived consequence of that same add: the key is
inserted immediately after `verified_by`, or immediately before `contradiction`
(the `SetOrAppend` order `ContradictInvariantStatement` produces). No key was
removed, no value changed, no type changed — anything else would have been
**BLOCKED** and reported instead.

**Event logs.** `event_hash`/`prev_hash` churn is mechanical, and was proven so
rather than assumed: an independent Python reimplementation of
`state.eventHash` (sha256 over canonical `json.dumps(sort_keys=True)` of
`{seq,at,type,ref,data,prev_hash}`) reproduces **both** the pinned and the
captured chains byte-exactly, and the only non-hash delta per event is the
sanctioned key. No hash was hand-edited.

## 3. Deviation from the brief: five files, not four

The brief named four testdata files. **`scenario_steps.json` is a fifth pinned
expectation carrying the identical, sanctioned delta** (10 `verified_by`
entries, in the per-step `links` snapshots for steps 5–12). It is a *read*
fixture (`goldenValue(t, "scenario_steps.json")` compared per step with
`validation.DumpIndented`), so leaving it stale keeps
`TestLinkScenarioMatchesGoPin` failing and the stated gate cannot reach exit 0.

This is not "another delta" (the brief's BLOCKED condition) — it is the same
sanctioned additive key in an additional pinned expectation, which the ruling
covers. It was regenerated by replaying the identical scenario call sequence and
is included in the commit. **Flagged here for the orchestrator to confirm.**

## 4. What changed

- **Re-pinned (5):** `testdata/{scenario_links.json, scenario_events.jsonl,
  guard_links.json, guard_events.jsonl, scenario_steps.json}`.
- **Renamed (2):** `TestLinkScenarioMatchesPythonTwin` →
  `TestLinkScenarioMatchesGoPin`; `TestGuardMessagesMatchPythonTwin` →
  `TestGuardMessagesMatchGoPin`. Each carries an honest header comment: the
  Python twin was **retired at P4**, so there is no longer a Python
  implementation to agree with — these fixtures pin **GO** behaviour; the
  `verification_method` key is the Task 4A additive attestation disclosure
  (commit `521eecfd`), deliberately re-pinned, reason in the commit body.
- **Helper comment:** `compareTwinFile` keeps its identifier (renaming it was
  outside the brief's scope and it has exactly two call sites) but its doc
  comment and failure text now say *pinned Go expectation* rather than *Python
  twin*. The old tests are gone: `go test -run 'TestLinkScenarioMatchesPythonTwin|TestGuardMessagesMatchPythonTwin'`
  reports "no tests to run".
- **Parked residual fixed:** `internal/cli/cmd_invariant_verify.go` help line
  `mark an invariant verified` → `record an operator attestation (attribution,
  not mechanical proof)`. No test or fixture pinned the old string.
- `gofmt -w` was needed on that one file (the concatenation changed the
  `run:` alignment); the CLI package was re-tested after formatting.

## 5. Gates (all exit 0)

| gate | result |
|---|---|
| `go test ./internal/invariants ./internal/cli -count=1` | exit 0 |
| `go test ./... -count=1` | exit 0 — 71 `ok`, **no** FAIL/panic |
| `go vet ./...` | exit 0, no output |
| `gofmt -l internal/ cmd/` | empty |
| `go test ./internal/invariants -run 'Test…GoPin' -count=1 -v` | both **PASS** |
| `go test ./internal/state -run Legacy -count=1` | `TestEventLegacyAnchor`, `TestVerifyLogLegacyOnly` PASS |
| verify-full step 9 (standalone replay) | `audit PASS` … `invariant_verification=0 problem(s)`, `audit --json ok: true`, `verify ok: true` |

**Legacy untouched — confirmed:** `git status --short scripts/` is empty,
`git diff HEAD -- scripts/legacy/` is empty, and the tree hash is unchanged
(`f62c95c0a947f94e15328ee0d08fc1b70ab31ec6`). The Python-era campaign still
audits clean, including §11 `invariant_verification=0 problems`, so the additive
key does not disturb reading reference-written state.

## 6. Commit

Eight paths, staged by exact path (nothing else in the tree):

```
internal/cli/cmd_invariant_verify.go
internal/invariants/guard_test.go
internal/invariants/invariants_test.go
internal/invariants/testdata/guard_events.jsonl
internal/invariants/testdata/guard_links.json
internal/invariants/testdata/scenario_events.jsonl
internal/invariants/testdata/scenario_links.json
internal/invariants/testdata/scenario_steps.json
```

`f13ae1df` — 8 files changed, 86 insertions(+), 27 deletions(-). The body states
the ruling it relies on, the two read paths that make the key additive, the
byte-delta proof, the event-hash re-chaining proof, the five-file scope note, the
rename rationale, the help-line fix and the full gate list.

## 7. Transcripts

`.scratch/sdd/task-4a-logs/` — `fix1-baseline-failures.log`,
`fix1-delta-check.log`, `actual/` (captured bytes + per-step golden/actual
dumps), `fix1-focused-gate.log`, `fix1-full-test.log`, `fix1-vet.log`,
`fix1-gofmt.log`, `fix1-legacy-state.log`, `fix1-legacy-step9.log`,
`fix1-cli-postgofmt.log`, `fix1-renamed-tests.log`, `fix1-commit-msg.txt`.
