# Wave J Implementation Plan (definitive final round — production-ready)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close every remaining useful item, correct all known doc drift, prove the full 13-step gate green (including `-race` and the determinism double-run, neither of which any wave has run since 2026-09-10), add CI so the gate runs without this box, and merge. After this wave there is no backlog: Wave H is empty, the methodology's self-application gaps are closed, and the doc headers tell the truth.

**Basis (evidence, not ambition):** three parallel audits (skip-lies, H/TODO triage, dead-code/surface) + controller-verified gaps (per-class reporting, stale Wave E/header prose, suite compiler monoculture) + the running `scripts/verify-full.sh` result, all dated 2026-09-11. Tasks marked *(audit-gated)* take their file lists from the audit reports; if an audit returns "clean" for an area, its task SHRINKS to a one-line ledger entry, never expands.

**Architecture:** Same posture as Waves G/I. No new verbs, no new flags (one exception if an audit proves a need — none is pre-authorized). Presence-gated renders; fail-open advisories; policy-gated behavior. Golden byte discipline throughout.

**Tech Stack:** Go control plane (`webv2`), embedded JSON/YAML assets, `validation.Value` documents, golden suite green throughout.

## Global Constraints

- Golden byte discipline: `scripts/golden.sh` exit 0 after EVERY task. Never edit golden fixtures; a move is BLOCKED-report.
- Env: `GOCACHE=$PWD/.gocache`; `rg` never grep; never `git add -A`; stage only task files.
- After ANY `assets/` edit: `python3 scripts/sync-asset-manifest.py`, stage manifest.
- TDD failing-first; byte-law tests against literal fixtures; sorted output; no wall-clock in new read paths; no `os.Stat` mtimes.
- Model for workers: implementers `provider:"deepseek-official", model:"deepseek-flash"`; reviewers `provider:"opencode-go-responses", model:"muse-spark-1.3-contributor"` in PLAIN TEXT (no `opts.schema` — schema-constrained calls return null on that provider; parse the `VERDICT:` first line yourself).
- Untracked user files at repo root NEVER staged/committed.
- verify-full.sh is the gate of record for this wave (13 steps). Task gates run the focused tests + golden.sh; the CLOSE-OUT task runs verify-full.sh end to end.

---

### Task 1: J-docs — stale-prose correction (header, Wave E, landing tables)

*(audit-gated: H/TODO audit confirms the exact stale lines; controller pre-verified the three below)*

**Files:**
- Modify: `docs/IMPROVEMENTS.md` (header block lines 1–22; Wave E status block ~line 1565; implementation-order table rows 17–18 for E1/E2)

**Corrections (locked — each is a statement of landed fact, not a judgment; G14 evidence: `4edfd60` amend/supersede + live `internal/cli/cmd_amend.go`, `b8ed2c6` batch dispose + `internal/planner/batch.go`, `7230c42` shared runner; D8 evidence: `85b2e3d`):**
1. Header: `E1–E4 deferred by principle 6` → `E1/E2 LANDED via G14, E3/E4 deferred by principle 6`; `D8 awaits a decision` → `D8 LANDED 2026-09-10`; append `Wave H backlog filed 2026-09-11 (15 items, open) — Wave I LANDED 2026-09-11 (I1–I6)`.
2. Wave E status block: `E1–E4 remain recorded, not scheduled` → `E1/E2 CLOSED by G14 (amend/supersede + batch dispose, tranche 2); E3/E4 remain recorded, not scheduled`.
3. Implementation-order rows 17–18: mark E1/E2 `LANDED via G14` (keep the rows — the table is a history, not a todo list).
4. E1/E2 section bodies: E1 (`### E1.`) `No path exists to amend…` → point at `webv2 amend` + `SUPERSEDED` (G14); E2 (`### E2.`) `The probes CLI is run/list/blank only` → point at the batch-dispose path (`internal/planner/batch.go`). One sentence each — the sections stay as the historical ask, but must not describe a missing capability that exists.

- [ ] **Step 1: Failing test** — there is no test for prose; the failing-first proof is a `rg` showing the stale strings present, then absent. Pin the three corrected sentences as literal greps in the task report (before/after).
- [ ] **Step 2: Run** — show the stale lines.
- [ ] **Step 3: Implement** — the three corrections, nothing else. Do NOT renumber anything, do NOT touch Wave H or Wave I sections.
- [ ] **Step 4: Run** — `rg` confirms the stale strings are gone and the new strings present; `scripts/golden.sh` green (docs-only, but prove it).
- [ ] **Step 5: Commit** — `docs(J): correct stale Wave E/header status — E1/E2 closed by G14, D8 landed, Wave I recorded`

---

### Task 2: J-perclass — per-class precision/recall in `## eval` (methodology self-application)

**Why (locked):** `docs/eval-methodology.md` checkpoint 3 and hard-kill #4 demand per-class metrics and per-class-best-detector baselines of every external claim we ingest. Our own `## eval` reports aggregate recall/precision only — we do not eat our own cooking. This task closes that gap for the suite side (per-class baseline comparison rides on it as stated in follow-ups).

**Files:**
- Modify: `internal/evalscore/evalscore.go` (extract per-class scope helper if needed; `ScoreSuite` byte-identical)
- Create: `internal/evalscore/classes.go` + `classes_test.go`
- Modify: `internal/audit/sections/eval.go` (presence-gated block + value keys)
- Modify: `internal/audit/sections/eval_test.go` (+ dependent pins as in Task I3)

**Design (locked):**
```go
// ClassRow is one bug_class cell over the ScoreSuite scope.
type ClassRow struct {
    Class            string // gold.bug_class, lowercased
    Cases, Hits      int    // matched gold cases of this class, hits among them
    Live, Anchored   int    // live findings attributed to this class, anchored among them
    RecallLine       string // wilson.Format(Hits, Cases, "recall")
    PrecisionLine    string // wilson.Format(Anchored, Live, "precision")
}
func Classes(programs []string, liveByProgram map[string][]validation.Value,
    cases []validation.Value) []ClassRow // sorted by Class; classes with zero cases never appear
```
- Scope and anchor rule = the SAME helpers `ScoreSuite` uses (one implementation). A live finding is attributed to a class by `root_cause.class` (lowercased; empty/missing class ⇒ attributed to `unmapped` and counted — never dropped silently).
- n=1 cells render their Wilson intervals honestly (`1/1 (95% CI 20.7–100.0%)`); the header line says `per-class cells are small — read the intervals, not the ratios` so nobody misreads a 1/1 as certainty.
- Presence gate: block renders only when the scope has ≥1 live finding AND ≥1 matched case (same gate family as I3); value key `classes` (array of `{class, cases, hits, live, anchored, recall, precision}`) absent when gated off.
- Rendered after the fabrication ledger block:
```
- recall/precision by gold class (small cells — read the intervals, not the ratios):
  - access-control: recall 1/1 (95% CI 20.7–100.0%), precision 1/2 (95% CI 9.5–90.5%)
```
- Golden safety: `ErrSkip` campaigns unaffected; zero-live campaigns byte-identical (unit-pinned).

- [ ] **Step 1: Failing tests** — per-class attribution incl. the `unmapped` bucket; sorted order; n=1 intervals pinned literally; block byte-absent when clean; value round-trip.
- [ ] **Step 2: Run** — expect FAIL (undefined `Classes`).
- [ ] **Step 3: Implement** — per above.
- [ ] **Step 4: Run** — focused + full + golden green.
- [ ] **Step 5: Commit** — `feat(J): per-class recall/precision in ## eval — methodology checkpoint 3 self-applied`

---

### Task 3: J-diversity — suite compiler-version diversity (explicit report ask)

**Why (locked):** the source report explicitly asks for "contracts compiled with different, non-default compiler versions". All 17 suite fixtures carry `pragma solidity ^0.8.24` (ES07's 0.8.19 note is a comment; it still compiles 0.8.24). This task adds TWO fixtures with older pragmas, proving the suite is not a single-compiler monoculture.

**Files:**
- Create: `assets/evalsuite/src/ES18...sol`, `assets/evalsuite/src/ES19...sol` (one `pragma solidity ^0.8.0`, one `pragma solidity >=0.7.0 <0.9.0` — both must still compile under the repo's solc so slither/aderyn/binary-gated tests keep passing)
- Modify: `assets/evalsuite/cases.json` (two new rows, CASE- ids next free, `partition` matching the suite's existing split discipline, honest `deployed_at`)
- Modify: `internal/evalstore/deployed_test.go` (row-count pin 17 → 19, if it pins the count)
- Modify: `assets/testdata/asset_manifest.json` (regen)
- Any suite-count pins elsewhere (grep `17` in evalsuite-adjacent tests and update exactly those)

**Constraints (locked):** the new bugs must be REAL planted bugs in existing covered classes (no new taxonomy — this is a diversity task, not a coverage task); both must be flaggable by at least one existing archetype AND by slither or aderyn on this box (prove by running both binaries over the two files and pasting the hits into the report); `created_at`/`deployed_at` honest per Task I1a discipline (planted fixtures ⇒ copied dates, disclosed).

- [ ] **Step 1: Failing tests** — suite loads 19 rows; new rows validate; count pins updated.
- [ ] **Step 2: Run** — expect FAIL (17 ≠ 19).
- [ ] **Step 3: Implement** — fixtures + rows + pins + manifest sync.
- [ ] **Step 4: Run** — focused + full + golden green + binary proof (slither + aderyn hit both new bugs).
- [ ] **Step 5: Commit** — `feat(J): suite compiler diversity — two pre-0.8.24 fixtures, both tool-flaggable`

---

### Task 4: J-backlog — the validated H items (H1, H15, + audit-confirmed)

*(triage COMPLETE 2026-09-11: all H1–H15 STILL-OPEN — item list below is final; update the Wave H section rows to STRUCK/LANDED with commit hashes as items land, the section stays as history)*

**Files:** per the audit report (controller pastes the validated list at dispatch).

**Locked (triage-validated 2026-09-11 at main f4b6c66 — ALL 15 STILL-OPEN, zero already-fixed; TODO/FIXME/XXX/HACK inventory: zero real items, all hits are by-design placeholder vocabulary or fixture content):**
- H15 (`reentrancy → SWC-107`): one data line + four pinned literals (`internal/report/aliases_test.go:36`, `internal/briefing/aliases_test.go:58`, `internal/classweights/aliases_test.go:92` — all `"[OWASP SC05]"`) + manifest regen. No code change.
- H1 (unattributed cost edge): emit gate `internal/costs/costs.go:430` is still `if unattributedRows > 0 || !planOK` — add `|| planned["unattributed"]>0` + test that shape.
- H2/H3/H4 (postpatch_scope caps + shared constant): `scopeSolFiles` (`internal/reproduction/postpatch_scope.go:97`) needs a file-count/bytes guard; `PlantCheck` rows need the overflow cap; `scopeExcludedDirs` (`:41-43`) → shared constant with `internal/structidx/parser.go:52-53`.
- H5 (dedicated citation hash fields): schema migration — `affected[].hash`-style fields for `mitigation_present`/component citations. LARGEST item; keep additive (optional fields, existing string shapes stay).
- H6 (ES16/ES17 presence asserts), H7 (nested-index write regex), H8 (shared axes list), H9 (per-line Suite-result match), H10 (registry test to ten playbooks), H11 (immunefi hygiene), H12 (`strconv.Itoa`), H13 (record the number-strictness decision — fixtures verified clean, so this is a decision + comment, not a migration), H14 (conditional hold — revisit condition unmet ⇒ record the hold, do not change code).
- Batching: behavioral items (H1, H2, H3, H5, H7) one commit each; hygiene (H4, H6, H8, H9, H10, H11, H12, H13, H14, H15) may batch max 3 per commit, each item named in the body.

- [ ] **Step 1: Failing tests** — per item, as specified at dispatch.
- [ ] **Step 2: Run** — expect FAIL.
- [ ] **Step 3: Implement** — item by item, one commit per behavioral item, hygiene batched: `fix(J): H<n> …`.
- [ ] **Step 4: Run** — focused + full + golden green per commit.
- [ ] **Step 5: Commit(s)** — as above; update the Wave H section rows to STRUCK/LANDED with commit hashes (the section stays as history).

---

### Task 5: J-lies — skip-gate + TODO + dead-code sweep (audit-gated)

*(audit-gated: file list = whatever the skip/dead-code audits prove is a lie or dead; if both return clean, this task SHRINKS to striking nothing and recording the negative result in the ledger — the task still exists so the negative result is on the record)*

**Locked (known now):** nothing is pre-authorized for deletion. Every deletion needs the audit's byte-proof (no importers via `go list -deps`, no runbook/walkthrough reference, full suite green before AND after).

- [ ] **Step 1: Failing tests** — for each lie: a test or gate assertion that fails while the lie lives (e.g. a test asserting the env-gated suite ran N tests, a `go list -deps` assertion). For each dead symbol: the deletion itself is the proof (build green after).
- [ ] **Step 2: Run** — expect FAIL (or document the negative result).
- [ ] **Step 3: Implement** — wire the un-skipped gates / delete the dead / resolve REAL TODOs; STALE TODOs are deleted as prose (a TODO that describes done work is a lie in comments).
- [ ] **Step 4: Run** — focused + full + golden green.
- [ ] **Step 5: Commit** — `chore(J): …` (one commit; body lists each lie/dead/TODO with its proof).

---

### Task 6: J-ci — continuous gate (GitHub Actions)
**Why (locked):** every gate in this repo runs on one box (read-only home go-build cache, local tool binaries, local docker daemon). "Production ready" means the gate runs on a fresh clone without this laptop. This task adds the minimum CI that proves it.

**Files:**
- Create: `.github/workflows/ci.yml`
- (If the runner needs it) Modify: nothing else. If golden.sh or verify-full.sh hardcode box-specific paths, FIX THE SCRIPTS to respect env overrides (GOCACHE/GOPATH/TMPDIR) rather than special-casing CI.

**CI job (locked minimal):** `ubuntu-latest`, setup-go 1.26.x, setup-python, then:
1. `go build ./...`
2. `go vet ./...`
3. `go test ./... -count=1`
4. `bash scripts/golden.sh`
NOT in CI: `-race` (slow, flaky on shared runners — stays in verify-full.sh), docker e2e (daemon + images — stays local), binary-gated tool tests (they skip-gracefully without slither/aderyn — assert the skips, do not install the tools).

- [ ] **Step 1: Failing test** — the workflow file does not exist; the "test" is `git ls-files .github/workflows/ci.yml` non-empty + a YAML parse check (`python3 -c yaml.safe_load`, stdlib only — no new deps).
- [ ] **Step 2: Run** — expect FAIL (file absent).
- [ ] **Step 3: Implement** — the workflow + any env-override script fixes (each script fix proven by running that script with a scrubbed env locally).
- [ ] **Step 4: Run** — YAML parses; every CI command reproduced LOCALLY in order (build/vet/test/golden all exit 0 — the local run IS the CI simulation since there is no runner here); `git status` shows only the workflow file (+ script fixes if any).
- [ ] **Step 5: Commit** — `ci(J): minimal green gate — build, vet, tests, golden`

---

### Task 7: J-truthy — `pyTruthy` consolidation (the deferred "own pass")

**Why (locked):** `docs/feedback-triage.md` "Still open" table defers this explicitly: 21 definitions, 11 bodies, 4 divergent semantics — "a wrong unification silently flips gates. Needs its own pass that pins each call site's intended truthiness first." This is that pass. (Controller count 2026-09-11: 20 `func pyTruthy` + 1 `func pyTruthyCLI` across 21 files.)

**Files:** per the implementer's survey (any `*.go` file holding a `pyTruthy` clone or its call sites); no asset/schema changes expected.

**Method (locked — the failure mode is a silent gate flip, so the method IS the task):**
1. **Survey:** list every `pyTruthy` definition with its full body; cluster byte-identical bodies (hash them); diff the rest. Name the semantic variants found (the triage note says 4 — re-derive, do not trust the count).
2. **Pin first:** for EACH call site of a non-canonical variant, add/extend a unit test pinning the intended truthiness on the values that matter (empty string vs `"0"` vs `"false"` vs 0 vs empty array vs null — whichever the variant distinguishes). These tests must pass BEFORE any unification and must keep passing after.
3. **Unify only the proven-identical:** replace byte-identical clones with `validation.PyTruthy` (new export; document it as canonical format v1 per F1d — "matches CPython" retitles are out of scope, this is a new symbol so it ships with the Go-native contract from birth).
4. **Name the divergent:** variants with genuinely different semantics are NOT unified — they are renamed to say what they do (e.g. `pyTruthyLenient` / `pyTruthyStrict` / whatever the survey finds, with a doc comment stating the exact rule and where it applies). A visible divergence is a decision; a hidden one is a bug.
5. **Prove no gate moved:** full suite + golden green; additionally `rg -n "func pyTruthy\b"` must show exactly ONE definition afterwards (plus the distinctly-named variants, each with its call sites listed in the task report).

**Out of scope (locked):** `pyTruthyCLI` (CLI-layer fallback semantics — survey it, pin it, but do not merge it into the canonical form unless its tests prove identity); any behavior change at any call site (this task is a refactor — a test that needs its expectation changed is a STOP-and-report, not an edit).

- [ ] **Step 1: Survey + pinning tests** — variant table in the task report (body hash → files → semantic rule); new tests pin every non-canonical call site. Run: focused green.
- [ ] **Step 2: Run** — the new pinning tests pass against the CURRENT clones (they must — they pin existing behavior; a failure here means the survey misread the code).
- [ ] **Step 3: Implement** — canonical export + replacement of identical clones + renames of divergent ones.
- [ ] **Step 4: Run** — `rg` shows one canonical def; focused + full + golden green; `go vet` clean.
- [ ] **Step 5: Commit** — `refactor(J): pyTruthy consolidation — one canonical form, divergent variants named`

---

### Task 8: J-recovery — torn-log hand-recovery procedure + `probes --flag=value` parity

Two tiny production-operability items, one task, one commit. Both are cheap; both were explicitly left open; neither needs a design decision beyond what is written here.

**Half A — torn-log hand-recovery (runbook only, NO code, NO new verb).**
The `verify` log-repair verb stays DECLINED (a repair path needs a spec for what a repaired chain claims, and minting chain-rewriting semantics in the final round is exactly the wrong risk). What production needs instead is a documented recovery for the loud failure the framing guard already produces. Append to the runbook's `verify` section a `Torn log recovery (by hand, no tool)` procedure:
1. `verify` fails loud pointing at the byte offset — quote the exact error shape (read it from the code; do not invent it).
2. Copy `events.jsonl` aside (`cp events.jsonl events.jsonl.torn-<date>` — the torn copy is evidence, never deleted in the same step).
3. Truncate after the last complete line (last byte `\n`; the chain links each event to the previous hash, so a prefix truncated at a record boundary re-verifies — state this, and state the cost: events after the cut are LOST and must be re-done, and the loss is recorded in the campaign notes).
4. Re-run `verify` — it passes with fewer events; the torn copy stays beside the log.
Every step must be VERIFIED by the implementer against a real torn-log reproduction in scratch (crash-smoke already truncates `events.jsonl` in verify-full step 8 — reuse that technique, do not invent a new one). If any step does not reproduce as written, the procedure is wrong, not the log — fix the procedure.

**Half B — `probes --flag=value` parity (code, tiny).**
23 verbs accept the `=` form; `probes` does not (triage table: "cosmetic until someone scripts it" — scripting it is what production IS). Make `probes` accept `--flag=value` exactly like the 23 do: find the shared `=`-splitting helper the others use (there is one — the silent-failure fix references `looksLikeOption`; read how a converted verb does it and mirror it), add it to `probes`'s parse path, pin with a byte-exact test. If `probes`'s parser is structurally different such that the shared helper does not apply, STOP and report — do not hand-roll a second `=` convention.

**Files:**
- Modify: `assets/runbook/RUNBOOK.md` (half A procedure + half B legend touch if the runbook pins `probes` flag forms)
- Modify: `internal/cli/cmd_probes*.go` (half B only) + its test
- Modify: `scripts/runbook-walkthrough.sh` ONLY if it pins `probes` invocations affected by the change (check; the anchor test will fail loudly if a row goes stale — that is the mechanism working)

- [ ] **Step 1: Failing tests** — half B byte-exact `=` test FAILs; half A has no test (procedure) — its failing-first proof is the torn-log reproduction in scratch showing the CURRENT runbook has no recovery path (quote the dead end).
- [ ] **Step 2: Run** — expect FAIL (half B) + documented dead end (half A).
- [ ] **Step 3: Implement** — procedure + parity fix.
- [ ] **Step 4: Run** — focused + full + golden + walkthrough-anchor test green.
- [ ] **Step 5: Commit** — `feat(J): torn-log hand-recovery procedure + probes --flag=value parity`

---

### Task 9: J-closeout — full verify-full.sh, IMPROVEMENTS Wave J, merge

**Files:**
- Modify: `docs/IMPROVEMENTS.md` (Wave J section: per-item entries + audit negative results + the wave's premise corrections, if any; PLUS a `Wave J — declined with reasons` ledger closing the loop on every still-open ask so none reads as a forgotten todo: corpus score cap [operator's number], structidx↔probes authz vocab [coverage-contract change], E6 queue tie-break [deliberate + pinned], C1-as-amend [append-only store; supersede via G14 is the sanctioned spelling], C2 [low value], C4 [recommended against — G-01-at-scale machine], C5/E3 [deferred, needs an "explored" rule], C7/E4 [do-not-build reporting knob], `verify` log-repair verb [declined — hand procedure ships in J-recovery instead])
- Modify: `assets/runbook/RUNBOOK.md` ONLY if a task changed operator behavior (per-class block renders — one legend line if the runbook pins `## eval` lines; check first, touch only on need) + manifest sync if touched
- Ledger: `.superpowers/sdd/2026-09-11-wave-j/progress.md` closed out

**Full gate (in order — a failure anywhere re-opens the owning task, no exceptions):**
1. `GOCACHE=$PWD/.gocache go build ./...`
2. `GOCACHE=$PWD/.gocache go vet ./...`
3. `GOCACHE=$PWD/.gocache go test ./... -count=1`
4. `GOCACHE=$PWD/.gocache bash scripts/verify-full.sh` (the 13-step gate incl. `-race`, determinism double-run, golden, walkthrough — paste the step table + exit code)

- [ ] **Step 1: Docs** — Wave J section + runbook touch-if-needed + manifest + ledger.
- [ ] **Step 2: Gates** — the four commands above, in order, all green.
- [ ] **Step 3: Merge** — base == local main HEAD check (rebase + re-gate if moved), then `git checkout main && git merge --no-ff wave-j -m "Wave J: definitive close-out — …"`. Wave branch retained until user confirms.
