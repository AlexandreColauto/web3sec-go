# Wave M: fork consumption of bridged PoCs, scorecard fidelity, second real run Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development, one fresh subagent per task. Coders `b-ai/deepseek-v4.1-flash`, reviewers `b-ai/glm-5.3-flash`.

**Goal:** (1) make bridged witnesses actually runnable end-to-end (actor keys legal; `verify` emits a `poc-<INV>.json` artifact a real `webv2 sequence run` can execute, layout sidecar honored); (2) teach the scorecard what the first run exposed (correctly-clean agreement metric; cross-file tie-collision diagnostic); (3) correct my own wrong "gold-label drift" note; (4) collect a SECOND real evalsuite run with better claims (fast donation VIOLATED via twin, ES04 false-silence probe, remaining classes honestly attempted or documented out-of-scope).

**Constraints (bind every task):** plan law set from wave L-defer lines 11-21 of 2026-09-12-wave-l-defer-validation-completion.md applies verbatim: golden sacred, GOCACHE=$PWD/.gocache, gofmt/vet, manifest resync on embedded-asset edits, prompts untouched, no new verbs/flags (artifact FILE, not state, no schema change), presence-gating, fail-open, table-driven byte-pinned tests, no child processes/skips/sleeps in tests, explicit git adds (LEARNINGS.md foreign writer). Commits tagged `(M, n/5)`.

### Task 1: legal actor keys bridge-side
**Files:** `internal/harness/witness.go` (actor alias `actor-N` -> `actor_N`; docstring), tests incl. replacing `TestBridgedActorAliasesAreRefusedByTheRunPath` (c8e2a299) with the POSITIVE pin: a bridged doc now PASSES `LoadSequenceSpec` + `BuildCommand`-stage validation with its native aliases (no withLoaderSafeActorKeys rename in tests). Docs: §L4 landing note + RUNBOOK sentence if it names actor-N.
**Steps:** RED (existing byte pins change exactly in the alias spelling — list them), GREEN, full harness+sequencepoc+cli+audit tests, vet/gofmt; golden NOT needed unless a stored poc exists in fixtures (verify with rg; expect none). Commit `feat(harness): bridged PoC actor aliases are run-path legal (M, 1/5)`.

### Task 2: scorecard fidelity
**Files:** `scripts/minicertora-scorecard.py` (+self-test pins, docstring), README scorecard section.
- New column `clean_agreed`: gold rows `confirmed-not-exploitable` whose tied lines are ALL PROVEN (the specificity signal the first run showed ES17 deserved; JSON emits it too; TSV header updated; self-test fixture gains/derives a not-exploitable row — cases fixture may need one new row + expected-bytes repin).
- Tie-collision diagnostic: a rule-bearing line whose rule ties case X while its FILE STEM ties case Y -> line EXCLUDED with stderr naming both (prevents the donation_keeps_rate double-tie fabricating counts); tier order otherwise unchanged.
- Verify against committed first-run data: rerun `docs/minicertora-eval/2026-09-12` — expect ES17 now clean_agreed=1 (access-control row 2/0/0/1/1) with the ES06 line still excluded (collide row names it). Commit `feat(scripts): clean_agreed specificity column + cross-file tie collision exclusion (M, 2/5)`.

### Task 3: verify emits the runnable PoC artifact (+layout sidecar)
**Files:** `internal/cli/cmd_verify_harness.go` (counterexample rung WITH proof.calls bridging cleanly: write `artifacts/harness/<INV>/poc-<INV>.json` via the same artifact-write path `verify --scaffold` uses; presence-gated: nothing written when calls null/unbridgable; refusal -> file NOT written, existing poc: audit line still renders from state; OVERWRITE law: re-verify replaces file deterministically — byte-pin rerun idempotence), layout sidecar: before bridging, if `artifacts/harness/<INV>/layout.json` exists (operator-provided `{"<Contract>.<var>":"<slot>", ...}` object mapping to decimal slot strings — document exact shape), load+validate it (malformed -> ignore loudly via the refusal text `layout sidecar invalid: ...` in the record summary? NO — keep summaries stable: ignore + stderr note; document) and call `BridgeSequenceWithLayout`. Tests: e2e rows (counterexample+calls -> file bytes validated against sequence_poc schema via stored artifact; rerun idempotent; no-calls -> absent; bad layout sidecar -> plain bridge bytes + stderr note). Golden: file lives in campaign artifacts dir — golden campaigns never hit this path (no minicertora counterexamples stored; verify with rg). Commit `feat(cli): verify writes runnable bridged PoC artifacts with optional layout sidecar (M, 3/5)`.

### Task 4 (CONTROLLER, operator run — no subagent): second real evalsuite sweep
Use .scratch/mcvenv. Claims to author (grammar-legal; NO string literals in targets means twins ONLY where disclosed): fast donation twin (local-snapshot form — expect VIOLATED), ES04 credit-accounting PROVEN false-silence probe (gold bad, model blind = honest data), ES13 twin of ES04 (LegacySplitter balances sweep accounting), one reentrancy accounting attempt each for ES03/ES18 documenting out-of-scope honestly, ES11 via the MintFreeC-style donate twin (local `uint256 before = pool*1e18/supply; donate(e); assert ... == before;`). Fix the class-map, RESCORE with the M2 scorecard (clean_agreed + collision exclusion live on real data), write docs/minicertora-eval/2026-09-12-run2/ (results raw + claims + mechanics + updated table), RETRACT the wrong "gold-label drift" paragraph in run-1 claim-sources.md (ES17 IS the clean control: confirmed-not-exploitable; my note inverted it) and fix the architecture telemetry paragraph likewise. Commit `docs(minicertora): second real run + run-1 gold-label retraction (M, 4/5)`.

### Task 5: close-out
IMPROVEMENTS: Wave M record (fork consumption LIVE end-to-end minus RPC; scorecard specificity; run2 numbers; what remains: G3 backtest for gate moves, RPC-timeout handling in driver?, third run with template-surfaced state). Battery incl golden lock-safe + walkthrough + selftest + scorecard --self-test + manifest. Final whole-branch review (b-ai/glm-5.3-flash) over the wave range. Commit `docs: wave M close-out (M, 5/5)`.
